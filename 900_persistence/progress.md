# Progress

> Registro cronológico del avance del proyecto. La entrada más reciente va arriba.
> Cada agente (Claude, Codex, opencode, Gemini) debe leer este archivo al iniciar una sesión
> y actualizarlo al terminar un bloque de trabajo significativo.

## Índice

> Mantener sincronizado: al añadir/editar una entrada del historial, actualizar también su fila aquí.
> Para saltar a una entrada, buscar su ancla (`#` + título en minúsculas con guiones).

| Fecha | Hito | Estado | Ancla |
|---|---|---|---|
| 2026-07-21 | Cierre formal por decisión del humano de T-027/T-024/T-026 (D-037); T-050 cerrada como cubierta por T-057; T-056 degradada a en espera; nueva tarea T-061 (prueba de generalización multi-dominio) | completado | `#2026-07-21--cierre-formal-por-decisión-del-humano-de-t-027t-024t-026-d-037-t-050-cerrada-como-cubierta-por-t-057-t-056-degradada-a-en-espera-nueva-tarea-t-061-prueba-de-generalización-multi-dominio` |
| 2026-07-21 | Corrida 4 de T-027 EXITOSA de punta a punta (primera en cerrar así, ningún hallazgo L-005…L-009 reaparece; campo cerrado de actores de T-054 usado en producción real por primera vez); T-059 y T-060 completadas (sincronización raíz↔molde de `closing-protocol`, bug real en check A8 de `conformance.sh`); T-054 completada (campo cerrado de actores); L-025 (checkpoints intra-etapa saltados) y L-026 (falso negativo del Gatekeeper multi-condición) registradas; T-056 suspendida | completado | `#2026-07-21--corrida-4-de-t-027-exitosa-de-punta-a-punta-primera-en-cerrar-así-ningún-hallazgo-l-005l-009-reaparece-campo-cerrado-de-actores-de-t-054-usado-en-producción-real-por-primera-vez-t-059-y-t-060-completadas-sincronización-raízmolde-de-closing-protocol-bug-real-en-check-a8-de-conformancesh-t-054-completada-campo-cerrado-de-actores-l-025-checkpoints-intra-etapa-saltados-y-l-026-falso-negativo-del-gatekeeper-multi-condición-registradas-t-056-suspendida` |
| 2026-07-20 | `repository.default_branch` (campo declarado, nunca consumido) cableado en el bootstrap de `git-protocol.md`, check B0 en `conformance.sh` y aviso pre-push en `closing-protocol` (T-058 resuelta, D-036); criterio de L-005 corregido sobre procedencia declarada (T-052); lección L-024 (declarar ≠ consumir) y deuda T-059 (deriva raíz↔molde de `closing-protocol`) | completado | `#2026-07-20--repositorydefault_branch-campo-declarado-nunca-consumido-cableado-en-el-bootstrap-de-git-protocolmd-check-b0-en-conformancesh-y-aviso-pre-push-en-closing-protocol-t-058-resuelta-d-036-criterio-de-l-005-corregido-sobre-procedencia-declarada-t-052-lección-l-024-declarar--consumir-y-deuda-t-059-deriva-raízmolde-de-closing-protocol` |
| 2026-07-20 | `Bash` a los cuatro agentes de etapa (T-048, alcance ampliado a invariante+check por agente), `_tools/conformance.sh` primer ejecutable del molde (T-057, validado contra las tres corridas de T-027), hallazgo nuevo L-023 (confirmación en una sola escritura), precondición `git init` retirada de `T-027_procedimiento.md` (T-047 resuelta, D-035, A-004 confirmada), smoke test parcial del bootstrap con hallazgo de rama `master` vs `main` (T-058 nueva) | completado | `#2026-07-20--bash-a-los-cuatro-agentes-de-etapa-t-048-alcance-ampliado-a-invariantecheck-por-agente-_toolsconformancesh-primer-ejecutable-del-molde-t-057-validado-contra-las-tres-corridas-de-t-027-hallazgo-nuevo-l-023-confirmación-en-una-sola-escritura-precondición-git-init-retirada-de-t-027_procedimientomd-t-047-resuelta-d-035-a-004-confirmada-smoke-test-parcial-del-bootstrap-con-hallazgo-de-rama-master-vs-main-t-058-nueva` |
| 2026-07-20 | Cotejo corrida 2 ↔ corrida 3 del mismo fixture: la divergencia es subdeterminación (L-022) y entrada no controlada (L-021), no azar; cuatro tareas de determinismo y observabilidad T-054…T-057 | completado | `#2026-07-20--cotejo-corrida-2--corrida-3-del-mismo-fixture-la-divergencia-es-subdeterminación-l-022-y-entrada-no-controlada-l-021-no-azar-cuatro-tareas-de-determinismo-y-observabilidad-t-054t-057` |
| 2026-07-20 | Corrida 3 de T-027 (`App_Reciclaje_3`) evaluada: FRACASADA por reaparición de L-009 en la etapa de prototipo, con L-006/L-007/L-008/G7 cerrados con evidencia y L-005 pasando con criterio desfasado; cuatro lecciones nuevas L-017…L-020 y seis tareas T-048…T-053 | completado | `#2026-07-20--corrida-3-de-t-027-app_reciclaje_3-evaluada-fracasada-por-reaparición-de-l-009-en-la-etapa-de-prototipo-con-l-006l-007l-008g7-cerrados-con-evidencia-y-l-005-pasando-con-criterio-desfasado-cuatro-lecciones-nuevas-l-017l-020-y-seis-tareas-t-048t-053` |
| 2026-07-19 | Cierre de T-042/T-043/T-044/T-041/T-046/T-045 (desacople commit↔gate, contrato de entrada entre etapas, `project.yaml` con procedencia, alineación de "cubierta" en tres skills, criterio de L-007 corregido, checkpoints intra-etapa), D-033 (A-003 resuelta: checkpoints locales) y D-034 (contrato de entrada como paso 0 de `methodology.md` §5.1); L-011 ampliada con hallazgo del tercer skill | completado | `#2026-07-19--cierre-de-t-042t-043t-044t-041t-046t-045-desacople-commitgate-contrato-de-entrada-entre-etapas-projectyaml-con-procedencia-alineación-de-cubierta-en-tres-skills-criterio-de-l-007-corregido-checkpoints-intra-etapa-d-033-a-003-resuelta-checkpoints-locales-y-d-034-contrato-de-entrada-como-paso-0-de-methodologymd-51-l-011-ampliada-con-hallazgo-del-tercer-skill` |
| 2026-07-19 | Zona git de una pasada (`git-protocol.md`, T-030/T-034/T-035 completadas con reserva, D-031/D-032) y corrida 2 de T-027 (modalidad observador): FRACASADA por reaparición de L-009 (commit condicionado al gate humano), L-005/L-006/L-008 cerrados con evidencia, T-032 cancelada, cinco tareas nuevas T-042…T-046, cuatro lecciones nuevas L-013…L-016 | completado | `#2026-07-19--zona-git-de-una-pasada-git-protocolmd-t-030t-034t-035-completadas-con-reserva-d-031d-032-y-corrida-2-de-t-027-modalidad-observador-fracasada-por-reaparición-de-l-009-commit-condicionado-al-gate-humano-l-005l-006l-008-cerrados-con-evidencia-t-032-cancelada-cinco-tareas-nuevas-t-042t-046-cuatro-lecciones-nuevas-l-013l-016` |
| 2026-07-19 | Auditoría documentación↔ejecución (T-031 completada) y sus correcciones: tres arreglos de `ingest-protocol` (T-028), pasada de consistencia entre áreas (T-029), procedimiento de repetición de T-027 con criterios previos (T-033, D-030); ocho tareas nuevas T-034…T-041, tres lecciones nuevas L-010…L-012 | completado | `#2026-07-19--auditoría-documentaciónejecución-t-031-completada-y-sus-correcciones-tres-arreglos-de-ingest-protocol-t-028-pasada-de-consistencia-entre-áreas-t-029-procedimiento-de-repetición-de-t-027-con-criterios-previos-t-033-d-030-ocho-tareas-nuevas-t-034t-041-tres-lecciones-nuevas-l-010l-012` |
| 2026-07-19 | Prueba end-to-end T-027 (modalidad observador): Etapa 1 (ingesta) evaluada contra `ingest-protocol`, 5 hallazgos registrados (L-005…L-009), modalidad observador y no-corrección del fixture (D-029), tareas T-028…T-033 abiertas | en progreso | `#2026-07-19--prueba-end-to-end-t-027-modalidad-observador-etapa-1-ingesta-evaluada-contra-ingest-protocol-5-hallazgos-registrados-l-005l-009-modalidad-observador-y-no-corrección-del-fixture-d-029-tareas-t-028t-033-abiertas` |
| 2026-07-19 | Condición de entrada al estadio de Prototipo: ruta canónica `_prototype/` (D-028), detección de estadio en `startup-protocol` (Paso 4, propone-no-ejecuta), sección "Arranque de proyecto" en `AGENTS.md`, plantilla `client_brief_temp.md`, y corrección de dos gates documentados-pero-no-implementados (L-004) | completado | `#2026-07-19--condición-de-entrada-al-estadio-de-prototipo-ruta-canónica-prototype-d-028-detección-de-estadio-en-startup-protocol-paso-4-propone-no-ejecuta-sección-arranque-de-proyecto-en-agentsmd-plantilla-client_brief_tempmd-y-corrección-de-dos-gates-documentados-pero-no-implementados-l-004` |
| 2026-07-19 | Ruta documental del Descubridor: tercer agente `onboarding-reader` + skill `ingest-protocol`, entrevista solo-huecos y writer con dos insumos (D-027) — T-025 completada | completado | `#2026-07-19--ruta-documental-del-descubridor-tercer-agente-onboarding-reader--skill-ingest-protocol-entrevista-solo-huecos-y-writer-con-dos-insumos-d-027--t-025-completada` |
| 2026-07-18 | Prototipado Parte 2 (Prototipador): reubicación de la frontera humano↔agente (D-025) y materialización de `prototype-builder` + `prototype-protocol` con observabilidad §10 (D-026) — T-022 completada | completado | `#2026-07-18--prototipado-parte-2-prototipador-reubicación-de-la-frontera-humanoagente-d-025-y-materialización-de-prototype-builder--prototype-protocol-con-observabilidad-10-d-026--t-022-completada` |
| 2026-07-17 | Diseño de observabilidad/evaluación del Descubridor (perfiles de conformidad §10, D-023) y promoción de A-002 → D-024 (conservar `interview_document.md` como traza) | completado | `#2026-07-17--diseño-de-observabilidadevaluación-del-descubridor-perfiles-de-conformidad-10-d-023-y-promoción-de-a-002--d-024-conservar-interview_documentmd-como-traza` |
| 2026-07-17 | Prototipado Parte 2: plantilla `discovery_temp.md` y arquetipo Descubridor materializado como dos agentes (`onboarding-interviewer` + `onboarding-writer`) (T-021) | completado | `#2026-07-17--prototipado-parte-2-plantilla-discovery_tempmd-y-arquetipo-descubridor-materializado-como-dos-agentes-onboarding-interviewer-onboarding-writer-t-021` |
| 2026-07-17 | Cuarta ronda de `methodology.md`: gates + TDD en `state.yaml` (T-018), metodología cableada en `AGENTS.md`, y arranque de Prototipado Parte 1 (taxonomía de actores + Descubridor) | completado | `#2026-07-17--cuarta-ronda-de-methodologymd-gates-tdd-en-stateyaml-t-018-metodología-cableada-en-agentsmd-y-arranque-de-prototipado-parte-1-taxonomía-de-actores-descubridor` |
| 2026-07-17 | Tercera ronda de `methodology.md`: §7.1 estado por incremento (`state.yaml`) y §5.2 hermano de seguridad (T-017) | completado | `#2026-07-17--tercera-ronda-de-methodologymd-71-estado-por-incremento-stateyaml-y-52-hermano-de-seguridad-t-017` |
| 2026-07-17 | Segunda ronda de `methodology.md` (elimina prototipado por-incremento, observabilidad/evaluación, Revisor de código) y simplificación de `AGENTS.md` (T-016) | completado | `#2026-07-17--segunda-ronda-de-methodologymd-elimina-prototipado-por-incremento-observabilidadevaluación-revisor-de-código-y-simplificación-de-agentsmd-t-016` |
| 2026-07-17 | `principles.md` cableado como comportamiento vinculante y creación de `methodology.md` agnóstico (T-014, T-015) | completado | `#2026-07-17--principlesmd-cableado-como-comportamiento-vinculante-y-creación-de-methodologymd-agnóstico-t-014-t-015` |
| 2026-07-17 | Regla de protocolos vía agente y soporte de Gemini en `register-harness` (T-011) | completado | `#2026-07-17--regla-de-protocolos-vía-agente-y-soporte-de-gemini-en-register-harness-t-011` |
| 2026-07-17 | Provisión opencode (T-008), bootstrap-paradoja (T-009) y README de portabilidad | completado | `#2026-07-17--provisión-opencode-t-008-bootstrap-paradoja-t-009-y-readme-de-portabilidad` |
| 2026-07-17 | Validación real de la auditoría `register-harness` en `proyecto_prueba` | completado | `#2026-07-17--validación-real-de-la-auditoría-register-harness-en-proyecto_prueba` |
| 2026-07-17 | Primer versionado en git y skill `register-harness` para portabilidad multi-agente | completado | `#2026-07-17--primer-versionado-en-git-y-skill-register-harness-para-portabilidad-multi-agente` |
| 2026-07-17 | Inicialización del harness base | completado | `#2026-07-17--inicialización-del-harness-base` |
| 2026-07-17 | Capa de persistencia con índices y separación molde/instancia | completado | `#2026-07-17--capa-de-persistencia-con-índices-y-separación-moldeinstancia` |
| 2026-07-17 | CLAUDE.md de este proyecto | completado | `#2026-07-17--claudemd-de-este-proyecto` |
| 2026-07-17 | Protocolos de inicio/cierre, entregable `template/` autocontenido y contexto declarativo | completado | `#2026-07-17--protocolos-de-iniciocierre-entregable-template-autocontenido-y-contexto-declarativo` |

## Formato de entrada

```
### [YYYY-MM-DD] — <título breve del hito>
- **Estado:** en progreso | completado | bloqueado
- **Resumen:** qué se hizo en esta sesión.
- **Siguiente paso:** qué queda pendiente inmediatamente después.
- **Referencias:** archivos, tareas ([[tasks]]) o decisiones ([[decisions]]) relacionadas.
```

---

## Historial

### [2026-07-21] — Cierre formal por decisión del humano de T-027/T-024/T-026 (D-037); T-050 cerrada como cubierta por T-057; T-056 degradada; nueva tarea T-061 (generalización multi-dominio)

- **Estado:** completado

Sesión de decisiones sobre tareas pendientes, sin cambios de código. Continúa directamente sobre el cierre de la sesión anterior (corrida 4 de T-027 exitosa, ver entrada de abajo), resolviendo las preguntas que esa entrada dejó pendientes de decisión del humano.

**T-027, T-024, T-026 — CERRADAS por decisión del humano.** Criterio aplicado: la corrida 4 (2026-07-21, `App_Reciclaje_4`) cumplió literalmente el criterio escrito en `Base_Harness/T-027_procedimiento.md` (§3 criterios por hallazgo L-005…L-009, §4 criterios de la corrida completa, §5 "solo fracasa si reaparece algún hallazgo"). El procedimiento nunca exigió repetir la corrida para cerrar — eso habría sido una barra adicional no escrita, decidida después de ver el resultado. El humano decidió no exigirla y en cambio invertir el esfuerzo en pruebas de generalización multi-dominio (ver T-061 más abajo). Veredicto por hallazgo (ya narrado en detalle en la entrada del 2026-07-21 anterior): **L-005 a L-009 verificados sin reaparición** en la corrida 4. T-024 (prueba de `prototype-builder` end-to-end) y T-026 (prueba de la ruta documental end-to-end) se cierran por encadenamiento explícito: quedan cubiertas por el mismo resultado, sin evidencia adicional que aportar. Registrada como **D-037**: el criterio de cierre de una tarea de prueba es el texto de su procedimiento, no un número arbitrario de repeticiones no especificado.

**T-050 — CERRADA como cubierta por T-057.** Los checks B1-B4 de `conformance.sh` (T-057) ya detectan "salida de etapa sin commit" — fue justo lo que cazó en la corrida 3 de T-027. Lo único que T-050 pedía y T-057 no cubre (traza fina, orden intra-invocación, "¿leyó antes de escribir?") ya está asignado a T-039, que explícitamente dice "pierde urgencia, no relevancia" para ese resto. Decisión del humano: cerrar T-050 en vez de mantenerla abierta duplicando el alcance de T-039.

**T-056 — degradada a baja prioridad / en espera, no cancelada.** Se confirmó la decisión de ubicación si algún día se retoma (junto a `client_brief.md` en `Pruebas_Prototipado/`, fuera de este repo, mismo precedente que T-032 cancelada), pero el humano decidió que la estrategia principal para validar el harness ya no es repetir el mismo fixture con entrada controlada, sino probar generalización con proyectos de dominios distintos (T-061). T-056 queda en el backlog por si en el futuro hace falta aislar variables en un fixture específico.

**Nueva tarea T-061 — Prueba de generalización multi-proyecto.** Motivo explícito del humano: *"no quiero sentir que se está afinando solo hacia un proyecto específico [reciclaje]"*. Definición: correr el flujo completo (`client_brief` → `onboarding-reader` → `onboarding-interviewer` → `onboarding-writer` → `prototype-builder`) con 2 proyectos de dominios completamente distintos al de reciclaje — uno de ellos una app de espiritualidad/mentalidad sana (ejemplo dado por el humano); el segundo dominio queda por definir en una próxima sesión. El humano escribirá él mismo los `client_brief.md` de estos proyectos nuevos, no delegado a un agente. Criterio de éxito idéntico al de T-027 §5: la prueba fracasa **solo** si reaparece alguno de L-005…L-009 en un dominio nuevo; hallazgos nuevos específicos de cada dominio no son fracaso, son la ganancia esperada de la prueba (revelan si algún prompt asume implícitamente el dominio de reciclaje). No requiere el guion de entrevista fijado de T-056: cada dominio tiene su propio brief distinto por diseño, se busca cobertura amplia, no comparabilidad estricta entre corridas. Prioridad alta, siguiendo el patrón del resto de tareas de validación de T-027.

- **Siguiente paso:** definir el segundo dominio de T-061 en una próxima sesión; el humano escribirá los `client_brief.md` de ambos proyectos nuevos; ejecutar la prueba de generalización cuando estén listos.

- **Referencias:** `Base_Harness/T-027_procedimiento.md`, [[tasks]] T-027, T-024, T-026, T-050, T-056, T-061, T-057, T-039, [[decisions]] D-030, D-037, [[lessons]] L-005…L-009

### [2026-07-21] — Corrida 4 de T-027 EXITOSA de punta a punta (T-059, T-054, T-060 completadas; L-025, L-026 nuevas; T-056 suspendida)

- **Estado:** completado

**Hito principal: corrida 4 de T-027 exitosa.** El humano ejecutó la corrida 4 en `Pruebas_Prototipado/App_Reciclaje_4` (terminal separada; esta sesión actuó de apoyo/observador, sin tocar la memoria de ese proyecto). Resultado: **el estadio de Prototipo se completó de punta a punta** — ingesta → entrevista (8/10 preguntas) → discovery (aprobado con gate explícito, dos commits `65a94cf`→`43f4ffd`, patrón correcto de L-023: confirmación en escritura separada, no colapsada) → prototipo HTML clicable del actor Cliente/Generador únicamente (login simulado → sitios → solicitud → confirmación → estado de 6 hitos), con ajustes post-demo aplicados y verificados con Playwright. El bootstrap de git funcionó correctamente: la rama quedó en `main` conforme a `repository.default_branch`, confirmando la mitad de T-058 que seguía diferida (¿el AGENTE ejecuta el bootstrap? — sí). **Ningún hallazgo de L-005…L-009 reapareció**: es la primera de las cuatro corridas de T-027 en cerrar así. El campo cerrado de "Actores a construir" (T-054) se usó en producción real por primera vez — el discovery declaró explícitamente "Actores a construir: generador", sin ambigüedad, y el prototipo se ciñó a ese único actor. No se cierra T-027/T-024/T-026 por cuenta propia en este cierre: queda pendiente de decisión del humano si una corrida limpia basta o si el criterio exige más de una.

**T-059 completada — sincronización raíz↔molde de `closing-protocol`.** Deuda detectada al cierre de la sesión anterior (2026-07-20): la copia de raíz `.claude/skills/closing-protocol/SKILL.md` —la que efectivamente se ejecuta en `Base_Harness`— no tenía el paso de ejecutar `conformance.sh` (Paso 5.4) ni el aviso de rama contra `repository.default_branch` (Paso 6.4), que se habían añadido solo al molde (`template/.claude/skills/closing-protocol/SKILL.md`) en sesiones previas. Ambas copias quedan ahora idénticas. Misma clase de deriva que G10/T-002.

**T-054 completada — conjunto de actores como campo cerrado del discovery (cierra L-022).** Se añadió el campo "Actores a construir en este prototipo" en `template/_templates/discovery_temp.md` §5. Cambios en cadena: `discovery-protocol/SKILL.md` Paso 1.4 instruye rellenar el campo; `prototype-protocol/SKILL.md` (Paso 0.2 y Reglas invariantes) ahora lee y obedece ese campo en vez de asumir por su cuenta "solo el generador"; `methodology.md` §4.3 aclara que el "bajo demanda" es el valor por defecto del campo, no algo que el Prototipador infiera. No reabre T-022 (ya completada; esta tarea la complementa) ni cierra T-055 (verificación de reglas duras del Gatekeeper es un problema distinto).

**T-060 completada y L-026 registrada — bug real en el check A8 de `conformance.sh`.** Al evaluar el discovery real de la corrida 4 con la capa de conformidad, se encontró que A8 solo leía el "Umbral de éxito" del Gatekeeper en la **misma línea** del encabezado, dando falso negativo cuando el Gatekeeper multi-condición se escribe como lista numerada en líneas siguientes — exactamente el caso de la corrida 4 (24 ok/1 fallo, corregido a 25 ok/0 fallos tras el arreglo). Corregido en `template/_tools/conformance.sh`: ahora busca el primer valor numérico en las líneas siguientes al encabezado si la misma línea viene vacía. Verificado contra el discovery real de la corrida 4.

**L-025 registrada — checkpoints intra-etapa saltados en la construcción del prototipo.** El `prototype-builder` de la corrida 4 confirmó la primera versión del prototipo en un solo commit, sin los checkpoints intra-etapa que `git-protocol.md` §3.1 exige (commit por iteración ejecutable del bucle escribir→ejecutar→ajustar). No reabre L-005…L-009: es una observación de conducta a vigilar, sin tarea correctiva propia todavía.

**Hallazgo de proceso confirmado dos veces (no requiere lección nueva).** El gate de aprobación (discovery y prototipo) rechaza correctamente mensajes de segunda mano entre agentes: un agente orquestador no puede transmitir la aprobación del humano a otro agente que espera la palabra directa de este. Se observó primero en esta sesión (al intentar aprobar el discovery.md por transmisión) y se corroboró independientemente en la propia corrida 4 (el prototipo quedó con commits `[sin confirmar]` porque el cierre práctico del humano no fue la palabra literal esperada por cada artefacto). Es comportamiento correcto (P5/NC-6), no un defecto del harness.

**T-056 suspendida, sin decisión.** El guion de respuestas de entrevista como fixture versionado quedó sin trabajar en esta sesión: al preguntarle al humano dónde debía vivir el fixture (junto al brief en `Pruebas_Prototipado`, o en `Base_Harness` junto a `T-027_procedimiento.md`), decidió no trabajarla por ahora. Sigue pendiente en el backlog, sin resolver la ubicación.

- **Siguiente paso:** decisión del humano sobre si la corrida 4 exitosa cierra T-027 (y T-024/T-026 encadenadas) o si el criterio exige más de una corrida limpia; retomar T-056 cuando se decida la ubicación del fixture; considerar si L-025 (checkpoints saltados) merece una tarea correctiva propia si se repite.

- **Referencias:** `Pruebas_Prototipado/App_Reciclaje_4` (artefactos y `git log`, memoria propia no tocada desde aquí), `.claude/skills/closing-protocol/SKILL.md`, `template/.claude/skills/closing-protocol/SKILL.md`, `template/_templates/discovery_temp.md` §5, `template/.claude/skills/discovery-protocol/SKILL.md`, `template/.claude/skills/prototype-protocol/SKILL.md`, `template/_guideline/methodology.md` §4.3, `template/_tools/conformance.sh`, [[tasks]] T-027, T-024, T-026, T-054, T-055, T-056, T-058, T-059, T-060, T-022, [[lessons]] L-022, L-023, L-025, L-026, L-005…L-009

### [2026-07-20] — `repository.default_branch` cableado en el bootstrap (T-058, D-036), criterio de L-005 corregido (T-052), lección L-024 (declarar ≠ consumir), deuda T-059 (deriva raíz↔molde de `closing-protocol`)

- **Estado:** completado

**Origen: el humano corrigió una conducción defectuosa.** Al cierre de la sesión anterior, T-058 quedó planteada como pendiente de "decisión de diseño del humano" (qué rama usar en el bootstrap). El humano señaló que la pregunta estaba mal hecha: el dato **ya existía** en la fuente única de verdad — `_context/project.yaml` declara `repository.default_branch: main` desde el origen del proyecto (incluso en la plantilla del molde), y `AGENTS.md` lo anuncia como campo que los agentes leen. Pero ningún skill lo consumía: campo muerto, no decisión pendiente. Corregido en cuatro sitios.

**T-058 resuelta.**
1. `template/_guideline/git-protocol.md` §2 (bootstrap): ahora lee `repository.default_branch` e inicializa con `git init -b "<rama>"` (con alternativa `git init && git symbolic-ref HEAD refs/heads/<rama>` documentada para git < 2.28); si el campo falta, usa `main` por defecto. Informa al humano el nombre de la rama al inicializar (NC-6: nada de init silencioso). Añadido el razonamiento explícito: `git init` a secas toma `init.defaultBranch` de la configuración de la MÁQUINA que ejecuta el harness, así que la misma corrida produce `master` en un equipo y `main` en otro — el comportamiento del harness no debe depender del entorno de quien lo ejecuta (E1).
2. `template/.claude/skills/closing-protocol/SKILL.md` Paso 6: nuevo sub-paso 4 que compara la rama actual con `repository.default_branch` antes de empujar y **avisa** si no coinciden, sin renombrar por cuenta propia. De paso, corregida una renumeración rota (dos pasos "5" por un error de una sesión previa); el reporte final queda como paso 6.
3. `template/_tools/conformance.sh`: check **B0** nuevo (rama actual vs `default_branch` de `project.yaml`). Probado en ambos sentidos: `main` → CONFORME, `master` → FALLO.
4. `template/_guideline/methodology.md` §10.2: la fila de la familia de checks B menciona ahora la coherencia de rama.

Los comandos se verificaron empíricamente antes de prescribirlos (git 2.50.1: `git init -b` y la variante `symbolic-ref`, ambas correctas). **Decisión registrada como D-036.**

**T-052 completada — criterio de L-005 corregido en `T-027_procedimiento.md` §3.** Estaba desfasado frente a T-044 (sesión del 2026-07-19). Evidencia que lo confirmó: el fixture declara `name: ReciclApp` en `_context/project.yaml` y la cadena "ReciclApp" **no** aparece ni una vez en `client_brief.md`. El criterio antiguo (falla si aparece un nombre que no esté en el brief, p. ej. ReciclApp) habría puntuado como fabricación el comportamiento correcto: tomar el metadato de su carril (§0.2) y anotar su procedencia. Criterio nuevo, basado en **procedencia declarada y verificable**: pasa si el valor viene de `project.yaml` con su marca de fuente, o del brief con su localización, o es `<no declarado>`; falla si no está en ninguna fuente, o si está pero sin declarar de cuál viene. Lo que se caza es la fabricación y la procedencia opaca, no el nombre en sí. Aclaración registrada: el harness ya leía bien el nombre desde T-044; lo roto era el **criterio de evaluación de la prueba**, no el harness — son cosas distintas que se confundieron al principio de esta sesión.

**Ajustes derivados en `T-027_procedimiento.md`.** §2.5 "qué observar en la corrida": la rama esperada es ahora la de `repository.default_branch` (`main`); si sale `master`, el bootstrap no leyó `project.yaml`. Criterio de L-009, fila de bootstrap: añadido que quedar en `master` es **FALLO**, además del init silencioso.

**Lección nueva → L-024.** Un campo declarado en la fuente única de verdad, documentado en `AGENTS.md` como algo que los agentes leen, y que ningún skill consume, es peor que no tenerlo: da falsa confianza. `repository.default_branch` llevaba así desde su creación y solo se destapó al retirar la precondición `git init` (D-035), porque hasta entonces la rama siempre venía dada desde fuera. Patrón a vigilar: **declarar ≠ consumir** — conviene un check que verifique que cada campo de `project.yaml` tiene al menos un consumidor. Relacionada con L-015 (procedencia/carriles) y con T-057 (la capa de conformidad es donde iría ese check). Registrado también el fallo de conducción propio: presentar al humano como "decisión suya" algo que era un defecto con respuesta ya escrita en `project.yaml`; ante un hueco aparente conviene comprobar primero si la fuente única de verdad ya lo responde, antes de escalarlo como decisión (NC-6 mal aplicado: consultar no es gratis si la consulta es innecesaria).

**Estado para la corrida 4 (contexto, nada creado en esta sesión).** El humano montará `App_Reciclaje_4` manualmente; se le pidió explícitamente al agente no crearlo. Bloqueantes que quedaban: T-052 (resuelto en este bloque) y la sincronización del proyecto de prueba desde `template/` (paso 11 de §1, sigue pendiente y es del humano). Recomendado no bloqueante: T-056 (guion de respuestas como fixture) — sin él las diferencias entre corridas siguen siendo inatribuibles, aunque los criterios binarios L-005…L-009 resisten. Aviso registrado: `_context/project.yaml` debe existir ANTES de invocar al primer agente; si el bootstrap no lo encuentra cae en `main` por defecto y el check B0 no tendría contra qué comparar.

**Deuda detectada y no resuelta → T-059 (nueva).** Deriva raíz↔molde del `closing-protocol`: el enganche de `conformance.sh` (Paso 5.4) se añadió en la sesión anterior al `closing-protocol` del **molde** (`template/.claude/skills/closing-protocol/SKILL.md`), pero `Base_Harness` usa su propia copia en `.claude/skills/closing-protocol/SKILL.md` de la raíz, que no lo tiene. Consecuencia comprobada: el cierre anterior de hoy no ejecutó la capa de conformidad. Misma clase que G10/T-002. Nota: todos los cambios de hoy en `template/.claude/` (incluido el sub-paso 4 de rama) tienen esta misma deriva potencial hacia la copia de raíz.

- **Siguiente paso:** decidir/ejecutar T-059 (sincronizar la copia de raíz de `closing-protocol` con el molde, o formalizar cuál es la fuente de verdad que se ejecuta en este proyecto). Después, la corrida 4 de T-027 queda desbloqueada por el humano (sincronización manual del proyecto de prueba).

- **Referencias:** `template/_guideline/git-protocol.md` §2, `template/.claude/skills/closing-protocol/SKILL.md` Paso 6, `template/_guideline/methodology.md` §10.2, `template/_tools/conformance.sh`, `T-027_procedimiento.md` §2.5/§3, [[tasks]] T-058, T-052, T-059, T-057, [[lessons]] L-024, L-005, L-015, L-023, [[decisions]] D-036, D-035

### [2026-07-20] — `Bash` a los cuatro agentes de etapa (T-048), `_tools/conformance.sh` primer ejecutable del molde (T-057), hallazgo L-023, precondición `git init` retirada (T-047/D-035), smoke test del bootstrap (T-058)

- **Estado:** completado

**T-048 completada — más profunda de lo previsto.** Se añadió `Bash` a `onboarding-reader`, `onboarding-interviewer` y `onboarding-writer` (los tres que carecían de ella). Pero al revisar los cuatro prompts de agente completos (incluido `prototype-builder`, que ya tenía `Bash`) se encontró que **los cuatro eran mudos sobre git**: la obligación de commit vivía solo en los `SKILL.md`, nunca en el prompt del agente que lo ejecuta. Eso explica L-019 mejor que la teoría previa — `prototype-builder` tenía capacidad y aun así colgó el commit del gate, porque su propio prompt nunca se lo recordó como invariante. Alcance ampliado: cada uno de los cuatro agentes ganó (a) un paso explícito de commit en "Cómo operas", (b) un invariante en Principios ("la etapa cierra con commit, con gate o sin él" / "congelar es confirmar"), y (c) un check de conformidad nuevo que exige que `Bash` conste en la traza **del agente**, no del orquestador: R11 (reader), I13 (interviewer), W7 (writer), P10 (prototype-builder). Que el artefacto lo versione el orquestador ya no satisface el check — ese era el agujero que dejaba L-009 "sin evaluar" en tres de las cuatro etapas. Renumeraciones: R10→R11, I12→I13, P9→P10. Corregido el rango de checks citado en T-039 (`tasks.md`).

**T-057 completada — `template/_tools/conformance.sh`, primer ejecutable del molde.** Shell POSIX (dependencias `git`/`grep`/`sed`/`awk`); hasta hoy el molde era 100% Markdown+YAML — decisión de runtime y ubicación tomada por el humano. Tres familias de checks: **A·Artefactos** (procedencia del nombre, marcadores sin sustituir, tabla de cobertura, `n/a`/`parcial` justificados, coherencia estado↔confirmación, Gatekeeper cuantitativo, timebox), **B·Git** (commit por etapa con mensaje canónico, artefacto sin confirmar, doble escritura de la confirmación), **C·Definiciones** (lint agente↔skill: si el skill exige commit, el agente declara `Bash`; skills referenciados existen). Veredicto CONFORME/NO CONFORME, `exit 0/1/2`; avisos no alteran el veredicto.

**Validación contra las tres corridas reales de T-027.** Corrida 1 → detectó que no era repo git (hallazgo original de L-009). Corrida 2 → marcador sin sustituir + los tres agentes sin `Bash`. Corrida 3 → prototipo sin confirmar (L-009/L-019) + L-017 + un **hallazgo nuevo** (ver L-023 abajo). Dos falsos positivos propios detectados y corregidos durante la validación: (1) exigía igualdad exacta con `project.yaml`, marcando como fabricación un nombre legítimamente anotado con procedencia — castigaba justo lo que T-044 fomenta; corregido a contención. (2) El lint C1 matcheaba menciones en prosa en vez de invocaciones reales, señalando falsamente a `sesion-starter` por nombrar `closing-protocol` entre paréntesis.

**Enganche obligatorio.** Un check que nadie ejecuta es L-019 otra vez, así que se colgó de un paso obligatorio: `closing-protocol` Paso 5.4 lo ejecuta en cada cierre e incluye el veredicto en el resumen; **informa, no bloquea** (NC-6). Documentado en `methodology.md` §10.2 (nueva subsección) y en `AGENTS.md`.

**Límites a registrar con honestidad.** "CONFORME" significa "conforme a lo comprobable sin traza", no "el proyecto está bien". Y la herramienta no se ha probado sobre una corrida limpia — las tres disponibles fallan; que detecte fallos está demostrado, que no invente fallos sobre una corrida correcta solo se probó parcialmente (molde y proyecto vacío dan CONFORME). La corrida 4 es la primera prueba real de eso. El lint C1 pasa en el molde y falla en las tres corridas: prueba de regresión independiente de que T-048 funcionó.

**Hallazgo nuevo → L-023.** El `discovery.md` de la corrida 3 tiene **un solo commit**, ya como "cerrado", sin el borrador `[sin confirmar]` previo. Verificado a mano contra `git log` antes de darlo por bueno. El protocolo (`discovery-protocol` Paso 3 + `git-protocol.md` §3) exige confirmar ANTES del gate con el marcador, y que la aprobación produzca un segundo commit sin él. Se hizo en una sola escritura, así que en el historial un entregable aprobado es indistinguible de uno que nadie miró — se pierde justo la distinción que el gate quiere preservar. No estaba entre los 10 hallazgos de las corridas 1–3: lo encontró la capa nueva (check B6). Relacionada con L-007 y L-013 (misma clase: confirmación adelantada/colapsada).

**T-047 resuelta — decisión del humano (D-035).** Se retira la precondición `git init` de `T-027_procedimiento.md` §2.5. El proyecto de prueba arrancará **sin** repo git, para que el bootstrap de `git-protocol.md` §2 se ejercite de verdad — llevaba tres corridas sin ejecutarse nunca, siendo la pieza construida justamente para que L-009 no se repitiera. Red de seguridad que hizo viable retirarla: el primer check de `conformance.sh` es si el proyecto es repo git, así que un bootstrap fallido se detecta y se distingue de un defecto de diseño. **A-004 queda confirmada/resuelta.** `T-027_procedimiento.md` §2.5 invertida con su fundamento, añadido "qué observar en la corrida", y el criterio de aceptación de L-009 extendido con una fila de bootstrap escrita ANTES de correr (D-030): un `git init` silencioso cuenta como **FALLO**, porque cumple la letra y viola NC-6.

**Smoke test del bootstrap → T-058 (nueva).** Se probó la mitad comprobable (infraestructura: `git init` + `add` + `commit` sobre directorio virgen) — funciona. La mitad que importa (¿el AGENTE ejecuta el bootstrap?) no era comprobable en esta sesión porque `onboarding-reader` no está registrado como tipo de agente aquí; solo la responderá la corrida 4. **Hallazgo sin corregir:** `git init` creó la rama **master**, no `main`. `git-protocol.md` §2 no especifica el nombre de rama → queda a merced de `init.defaultBranch` de cada máquina. Consecuencia: un proyecto arrancado por el harness podría quedar en `master` mientras todo lo demás asume `main`, y el `push` del `closing-protocol` iría a una rama distinta según el entorno del cliente. La precondición retirada llevaba tres corridas tapando este hallazgo. No se corrigió: especificar la rama es decisión de diseño del humano. Prioridad alta — bloquea/afecta la corrida 4.

**Ajustes de estado de tareas existentes (sin cerrar por cuenta propia).** **T-050** se solapa fuerte con T-057 (sus checks B1–B4 ya detectan salida de etapa sin commit; es lo que cazó en la corrida 3). Queda **pendiente de decisión del humano** si se cierra como cubierta o se reduce a lo que necesita traza — no se cerró en esta sesión. **T-039** (motor de traza) pierde urgencia, no relevancia: la capa barata le quitó todo lo que no requería orden intra-invocación. **T-023** (re-sync a opencode/Gemini) crece con esta sesión: cuatro agentes modificados, `closing-protocol`, `AGENTS.md`, `methodology.md` y el nuevo `_tools/` quedan pendientes de propagar.

- **Siguiente paso:** ejecutar la corrida 4 de T-027 (ahora sin `git init` previo, con `conformance.sh` disponible) para responder si el bootstrap real lo ejecuta el agente y si la rama queda en `main` o `master`. Antes o junto con eso, decidir T-050 (¿se cierra por T-057 o se reduce?) y avanzar T-023 (deuda de re-sync ya alta).

- **Referencias:** `template/.claude/agents/{onboarding-reader,onboarding-interviewer,onboarding-writer,prototype-builder}.md`, `template/.claude/skills/closing-protocol/SKILL.md`, `template/AGENTS.md`, `template/_guideline/methodology.md` §10.2, `template/_tools/conformance.sh`, `T-027_procedimiento.md`, [[tasks]] T-048, T-057, T-047, T-058, T-050, T-039, T-023, [[lessons]] L-023, L-017, L-019, L-007, L-013, [[decisions]] D-035, [[assumptions]] A-004 (resuelta → D-035)

### [2026-07-20] — Cotejo corrida 2 ↔ corrida 3: la divergencia es subdeterminación, no azar (L-021, L-022) y cuatro tareas de determinismo/observabilidad

- **Estado:** completado

**Contexto.** Pregunta del humano: por qué dos corridas con la misma entrada en `_context/` dan resultados tan distintos, señalando un caso concreto — la corrida 2 construyó el prototipo del **generador** y la corrida 3 construyó **generador y operador**.

**La premisa era parcialmente falsa.** `client_brief.md` es idéntico (mismo md5, `6cff0022…`) y `project.yaml` solo difiere en la URL del repo. Pero `_context/` no es toda la entrada. Tres variables cambiaron:

1. **El harness** — 4 de 7 skills difieren (`ingest`, `interview`, `discovery`, `prototype`); los otros tres son idénticos. Los cuatro son exactamente los que tocaron T-041…T-045, y sus efectos se rastrean uno a uno: `Proyecto` pasó de `<no declarado>` a `ReciclApp` con procedencia (**T-044**); el extracto pasó de quedarse en `borrador` para siempre a dos escrituras separadas (**T-042**); y —el más revelador— los commits de la corrida 2 tienen **los cuatro el mismo timestamp** (16:33), es decir una reconstrucción retroactiva en lote, mientras que los de la corrida 3 están genuinamente distribuidos (18:49, 18:51, 18:58, 19:02…). **T-045 sí cambió el comportamiento**, aunque los ejecutara el orquestador (L-017).
2. **Las respuestas del humano** — causa dominante de la diferencia de profundidad, y **no es del harness**. El timebox lo fija el humano en Q1: "~30 min / 15 preguntas" en la corrida 2 vs "45 min / 20-25" en la 3 → 6 vs 12 preguntas efectivas → un discovery más denso → un prototipo de un `index.html` vs uno de 8 archivos con servidor. Ver **L-021**.
3. **Subdeterminación de la spec** — ver abajo.

**El caso generador/operador no fue azar (L-022).** `prototype-protocol` afirma tres veces que se construye solo el generador (líneas 45-46, 73, invariante 136). Pero el `discovery.md` de cada corrida decía otra cosa: en la 2, Operador *"ausente en este prototipo"* (el humano lo excluyó en Q4); en la 3, *"[bajo demanda, pero priorizado junto al generador]"* (el humano lo puso en el centro en Q1). **Los dos builders obedecieron correctamente a documentos distintos.** Cuando dos fuentes de verdad responden distinto a la misma pregunta, cada corrida elige una y ambas quedan justificadas. Es la misma clase de defecto que L-011, ahora sobre el conjunto de actores. No se arregla con temperatura ni con más disciplina de prompt.

**Distinción de método que orienta las tareas nuevas.** El harness en uso real **no puede ni debe ser determinista**: la entrevista es una conversación con una persona, y ante peticiones distintas la salida correcta es distinta — la variación es señal, no ruido. Lo que sí debe ser determinista es la **conformidad**: dado un discovery, lo que sale está determinado por él. Esa es la propiedad que falló.

**Cuatro tareas nuevas.** **T-054** (conjunto de actores como campo cerrado del discovery, obedecido por el skill — arregla el caso concreto), **T-055** (contrato verificable regla dura → evidencia en el borde discovery→prototipo), **T-056** (guion de respuestas versionado como fixture: sin entrada controlada no se puede medir si un arreglo funcionó, porque toda diferencia es inatribuible — reabre la discusión de T-032 con otro argumento), **T-057** (capa barata de conformidad).

**Sobre observabilidad (segunda pregunta del humano: cómo saber si los agentes actuaron correctamente).** El diseño **ya existe** y es correcto: `methodology.md` §10 (E13, P8, D-023) define traza por invocación, conformidad determinista sin LLM, y juez de calidad LLM aparte —con la línea que esta sesión confirmó tres veces: *"el auto-reporte del agente es narrativa, **no** evidencia"* (L-020)— y advierte que sin esa capa *"un fallo de comportamiento solo se detecta por inspección humana ad hoc"*, que es exactamente cómo se encontraron los 10 hallazgos de las tres corridas. Los checks por agente ya están escritos en los 4 prompts (R1-R10, I1-I12, W1-W7/T1-T5, P1-P9). **Lo que falta es solo el motor que los ejecute (T-039).** Hallazgo que reordena la prioridad: **8 de los 10 hallazgos eran detectables desde artefactos + `git log`, sin traza** — de ahí T-057, que da la mayor parte del valor a una fracción del costo.

- **Siguiente paso:** sigue siendo **T-048** (`Bash` a los tres agentes) como precondición de la corrida 4. Después, decidir el orden entre T-054 (barato, arregla un defecto conocido), T-056 (precondición de toda medición futura) y T-057 (observabilidad con el mejor ratio valor/costo).

- **Referencias:** `Pruebas_Prototipado/App_Reciclaje_2` y `App_Reciclaje_3`, `template/_guideline/methodology.md` §10, `template/.claude/skills/prototype-protocol/SKILL.md`, [[lessons]] L-021, L-022, L-011, L-017, L-020, [[tasks]] T-054…T-057, T-039, T-032, T-027, [[decisions]] D-023

### [2026-07-20] — Corrida 3 de T-027 evaluada: fracasada por reaparición de L-009, cuatro hallazgos nuevos (L-017…L-020)

- **Estado:** completado (la evaluación; la corrida queda fracasada y T-027 sigue en progreso)

**Contexto.** El humano ejecutó la corrida 3 en una terminal aparte, sobre `Pruebas_Prototipado/App_Reciclaje_3`, con el proyecto de prueba ya sincronizado desde `template/` (paso 11 de `T-027_procedimiento.md` §1, el bloqueante que quedaba). Esta sesión actuó como validador (modalidad observador, D-029): evaluó artefactos y traza git contra los criterios escritos **antes** de la corrida, sin ejecutarla ni corregir el proyecto de prueba.

**Veredicto por hallazgo (criterios de `T-027_procedimiento.md` §3).**

- **L-006 — cerrado.** El mejor resultado de la corrida. El extracto registra la contradicción de validación de correo como ambigüedad `A1`, **citando ambos extremos** (§6 y §9 del brief), nombrando las áreas en conflicto y **sin resolverla**. Y pasa la prueba de fuego de §4.4: el prototipo **no construye la validación que §8 excluye** — registro sin verificación, botones Gmail/Apple `disabled` con etiqueta "próximamente". La cadena completa respetó la exclusión.
- **L-007 — cerrado.** Dos escrituras reales y separadas: `087d2f3` crea el extracto en `Confirmado: no` / `Estado: borrador` con sufijo `[sin confirmar]` en el mensaje; `13d42d5`, dos minutos después, cambia **solo esas dos líneas** a `sí`/`cerrado`. El mecanismo de confirmación diferida y el marcador de T-042 se ejercitaron y se observaron.
- **L-008 — cerrado.** §3 y §10 en `n/a` con justificación; el interviewer no las preguntó; el writer dedujo §3 (tipo de prototipo razonado) y omitió §10 con constancia. Control inverso limpio: ninguna área que le toca al cliente aparece en `n/a`.
- **G7 — cerrado (primera ejecución).** Timebox acordado en Q1, antes de toda pregunta de área, con cifras reales (45 min / 20–25 preguntas; prototipo 15 días hábiles, feature freeze día 10). La entrevista cerró por **suficiencia declarada** en 11 preguntas, no por agotamiento.
- **L-005 — pasa, con el criterio desfasado.** `ReciclApp` no está en el brief, pero **sí** en `_context/project.yaml` (`project.name`), y el interviewer anotó la procedencia (`<!-- FUENTE: … -->`). Es exactamente la conducta que T-044 introdujo: no hay fabricación. El criterio escrito exige literalidad **en el brief** y fue redactado antes de T-044, así que marcaría como fallo un comportamiento correcto. Ver T-052.
- **L-009 — REABIERTO.** El prototipo se materializó y **no se commiteó**: `_prototype/prototype/` sin trackear. El `progress.md` del proyecto de prueba lo dice literalmente: *"prototipo materializado (pendiente de gate **y commit**)"*. Por §5 del procedimiento, la reaparición de un hallazgo de L-005…L-009 declara la **corrida fracasada**.

**Por qué este fallo es distinto de los dos anteriores.** En la corrida 2 el commit colgaba del gate **por diseño** (L-013) y T-042 lo corrigió. Aquí `prototype-protocol` Paso 4 ya dice lo correcto — *"el commit precede al gate porque el humano evalúa sobre un estado congelado e identificable"* — y `prototype-builder` **sí tiene `Bash`**. Spec correcta, capacidad presente, conducta incorrecta. Es la refutación del supuesto implícito de T-042: se dio por hecho que reescribir la regla cambiaría el comportamiento. De ahí L-019 y T-050: para una conducta que reaparece por tercera vez, la corrección ya no es otro cambio de prompt sino un **control que la detecte**.

**Reserva grave sobre los hallazgos cerrados.** Los commits de ingesta, entrevista y discovery **no los hicieron los agentes**: `onboarding-reader`, `-interviewer` y `-writer` declaran `tools: Read, Write, Edit, Glob, Grep, Skill`, **sin `Bash`**, así que la instrucción de commit de su propio skill es inejecutable. El reader lo reportó y el orquestador cubrió el hueco a mano (L-017). Consecuencia por la regla de L-016 (*camino no ejercitado ≠ camino correcto*): el commit por etapa **no se ejercitó en ninguna de las cuatro etapas** — en tres por incapacidad, en la cuarta por omisión. L-009 no está fallado en una etapa y cerrado en tres: está **sin evaluar** en tres y fallado en una.

**Hallazgos nuevos (§5: son para lo que sirve la prueba, no un retroceso).**

- **L-017** — tres agentes de onboarding sin `Bash` pese a que sus skills exigen commitear. Es el bloqueante de fondo de L-009 → **T-048**, precondición de la corrida 4.
- **L-018** — la ingesta perdió la convención de colores, que estaba íntegra en el brief; el interviewer gastó una pregunta del presupuesto limitado del humano en algo ya escrito, y **lo detectó el humano, no el harness**. Un artefacto confirmado no es un artefacto completo: la confirmación cubre lo que está, no lo que falta → **T-049**.
- **L-019** — el commit del prototipo colgado del gate pese a spec correcta y capacidad presente → **T-050**.
- **L-020** — el reporte del `prototype-builder` omitió dos comportamientos reales del motor (cuarta solicitud sin asignar, `zonaAmpliada=true` no declarada), encontrados solo al ejercitar el prototipo por API. El constructor fue honesto e incompleto a la vez → **T-051**.

**Nota de calidad de la corrida.** Pese al fracaso formal, es la corrida más productiva hasta ahora: llegó a prototipo funcional, resolvió las tres ambigüedades del brief con decisiones atribuidas al cliente, y su propia memoria registró cuatro lecciones útiles por cuenta propia — incluidas dos que delatan defectos del harness (L-017, L-018 en su numeración local). El proyecto de prueba se autodiagnosticó mejor de lo que el harness se verifica a sí mismo, que es justamente el hueco que T-039/T-050 deben cerrar.

- **Siguiente paso:** aplicar **T-048** (añadir `Bash` a los tres agentes) antes de cualquier corrida 4 — sin eso, L-009 seguirá sin poder evaluarse por mucho que se repita la prueba. Después, T-052 (criterio de L-005 desfasado) y decidir T-047. T-049/T-050/T-051 son los arreglos de fondo de los hallazgos nuevos.

- **Referencias:** `Pruebas_Prototipado/App_Reciclaje_3` (artefactos y `git log`), `T-027_procedimiento.md` §3–§5, [[tasks]] T-027, T-048…T-053, T-039, [[lessons]] L-017, L-018, L-019, L-020, L-005…L-009, L-013, L-016, [[decisions]] D-029, D-030

### [2026-07-19] — Cierre de T-042/T-043/T-044/T-041/T-046/T-045: desacople commit↔gate, contrato de entrada entre etapas, `project.yaml` con procedencia, alineación de "cubierta" en tres skills, corrección del criterio de L-007, checkpoints intra-etapa (D-033/D-034)

**Contexto.** Continuación directa de la sesión anterior: aplicar T-042…T-046 antes de repetir T-027 (corrida 3), todas dentro de `template/` (molde), sin commitear todavía.

**T-042 completada — desacoplar el commit de etapa del gate humano (cierra L-013).** `template/_guideline/git-protocol.md` §3: el disparador del commit pasa a ser la **salida de etapa**, no la aprobación del contenido — se confirma en cuanto el artefacto está escrito, con gate cruzado o no. Si el gate se cruza después, la aprobación produce su propio commit sin marcador. Nueva subsección §4 "Marcador de borrador": literal `[sin confirmar]` al final de la descripción, buscable con `git log --grep`; las etapas sin gate (`interview`) nunca lo llevan. Aplicado en `ingest-protocol` (Paso 5 + invariante) y `discovery-protocol` (Paso 3 + Paso 2.1 + invariante). `prototype-protocol` ya tenía el patrón correcto e `interview-protocol` no tiene gate: ninguno necesitó cambio.

**T-043 completada — contrato de entrada entre etapas (cierra L-014).** **Decisión de diseño (D-034):** en vez de un archivo `_guideline/` nuevo, el contrato entró como **paso 0 de §5.1 de `methodology.md`** ("Contrato de trabajo de un constructor de entregables"), que antes solo cubría la salida — una sola definición que los skills aplican, igual que `git-protocol.md` (D-032). Regla: leer `Estado`/`Confirmado por el humano` del insumo antes de consumirlo; si viene en borrador, **avisar** y dejar constancia en el artefacto producido, nunca bloquear ni confirmarlo por cuenta propia (NC-6); insumo cuyo estado no se pueda leer = no confirmado. Aplicada en `interview-protocol` (Paso 0.3 + invariante), `discovery-protocol` (Paso 0.4 + Paso 2.2 + invariante) y `prototype-protocol` (Paso 0.2 + invariante). `ingest-protocol` queda fuera a propósito: consume el `client_brief.*` del cliente, que no es artefacto del harness y no tiene campo de estado. Plantillas: `interview_temp.md` (*Extracto documental* admite "(sin confirmar)") y `discovery_temp.md` (campo nuevo *Procedencia de los insumos* en la Meta).

**T-044 completada — `project.yaml` como insumo declarado y regla de procedencia (cierra L-015).** `methodology.md` §0.2, nueva subsección "Dos clases de dato, dos fuentes": metadatos del proyecto ← `_context/project.yaml` (si falta, `<no declarado>`); contenido del cliente ← brief/entrevista (si falta, se pregunta); las fuentes no se cruzan, todo valor viaja con su procedencia declarada y esta se verifica antes de afirmarla. §5.1 paso 0 ampliado: `project.yaml` es insumo declarado de todo agente que escriba metadatos. Los cuatro skills de etapa (`ingest`, `interview`, `discovery`, `prototype`) leen ahora `project.yaml` en su Paso 0 + regla invariante propia (antes solo lo leían los protocolos de sesión, causa raíz de L-015). Tres plantillas anotan la fuente en el campo: los tres `**Proyecto:**` declaran `FUENTE: project.yaml (project.name)`; en `discovery_temp.md` el campo `Cliente / solicitante` declara lo contrario (viene del brief, no de `project.yaml`). **Asimetría deliberada** (decisión propia, no venía en T-044): un metadato ausente es `<no declarado>` en el extracto pero se PREGUNTA en la entrevista — el reader trabaja sin humano delante, el interviewer lo tiene ahí.

**T-041 completada — alineación de "cubierta" para §7 (cierra L-011, ampliada).** **Hallazgo:** la desalineación era de **tres** skills, no de dos — además del desajuste conocido (reader: métrica+umbral; writer: métrica+umbral+método), `interview-protocol` también elicitaba solo dos componentes, así que aunque el reader marcara `parcial`, el interviewer nunca habría preguntado el método. Corregir solo los dos de L-011 habría dejado el hueco abierto. Alineado **hacia arriba** (métrica + umbral + método de medición), porque §4.3 del canon exige una métrica *medible* y una métrica sin método no lo es. `discovery-protocol` Paso 1.6 queda como definición canónica, con nota de que cambiarla obliga a actualizar los otros dos skills en el mismo cambio. `ingest-protocol` e `interview-protocol` alineados; criterio anotado también en la fila §7 de la tabla de cobertura de `document_extract_temp.md`. L-011 se amplía en `lessons.md` con este hallazgo del tercer skill.

**T-046 completada — criterio de aceptación de L-007 (cierra L-016).** Archivo `T-027_procedimiento.md` (raíz, no `template/`): añadido el tercer caso de fallo — la confirmación **nunca ocurre** y el extracto termina en `no`/`borrador` (lo que pasó por alto la corrida 2). Añadida regla general en el preámbulo de §3: "camino no ejercitado ≠ camino correcto" — si la corrida no ejercita el mecanismo, el hallazgo se registra como **no evaluado** y sigue abierto. Actualizado el criterio de L-009: los commits `[sin confirmar]` que T-042 vuelve correctos ya no cuentan como fuera de convención. Actualizada la tabla de §1 (orden de aplicación): el paso 6 ya no es bloqueante, añadidos T-042/043/044/041 como pasos 7–10, y el sincronizado del proyecto de prueba pasa a ser el paso 11, único bloqueante de la corrida 3.

**T-045 completada — checkpoints intra-etapa (A-003 resuelta por el humano → D-033).** El humano decidió: los checkpoints intra-etapa quedan **siempre locales**; el push se hace solo al cerrar la sesión, contra el repositorio, respetando `auto_push`. `git-protocol.md` gana **§3.1 Checkpoints intra-etapa** (numerada así para no renumerar §4/§5, que ya tienen referencias apuntando): unidad = bloque natural (`interview` confirma por bloque de preguntas o al interrumpirse; `prototype` por iteración que deja algo ejecutable; nunca por Q&A suelto ni por edición de archivo); todo checkpoint lleva `[sin confirmar]`. `ingest` y `discovery` no llevan checkpoints intra-etapa (su artefacto se escribe en una sola pasada) — se corrigió una primera versión propia que les asignaba un checkpoint irrealizable. §5 reforzado: el "no empuja" aplica explícitamente a commits de etapa (§3) y checkpoints (§3.1). `interview-protocol` (Paso 1.5 + invariante) y `prototype-protocol` (Paso 2.4 + invariante) actualizados; en `ingest-protocol` se enlazó el Paso 4.1 al commit del Paso 5 (misma ambigüedad de orden ya corregida en `discovery`).

**Dos cuestiones abiertas, no decididas por el humano (registradas en `assumptions.md`, A-004; y en `tasks.md`, T-047/T-023 ampliada).** **(1)** La precondición §2.5 de `T-027_procedimiento.md` obliga a `git init` + commit inicial ANTES de invocar al primer agente, con lo cual el bootstrap de `git-protocol.md` §2 (el mecanismo que existe para que L-009 no se repita) **nunca se ejecuta** durante la prueba — la misma ceguera que la regla general recién codificada en §3, aplicada al procedimiento mismo. Quitarla haría la corrida más fiel; mantenerla evita que un fallo de infraestructura tumbe la prueba. **(2)** T-023 (re-sync a opencode/Gemini) acumula más deuda: todos los cambios de esta sesión tocan skills y guidelines que esas herramientas replican.

**Estado y siguiente paso.** T-041, T-042, T-043, T-044, T-045 y T-046 completadas. T-027 sigue **en progreso**: su único bloqueante para la corrida 3 es sincronizar el proyecto de prueba desde `template/` (paso 11 de `T-027_procedimiento.md` §1). Ningún commit se hizo durante la sesión de trabajo; el cierre de esta sesión es el primer commit que incorpora estos cambios.

Ref: [[tasks]] T-041, T-042, T-043, T-044, T-045, T-046, T-027, T-023, T-047 · [[decisions]] D-033, D-034 · [[lessons]] L-011 (ampliada) · [[assumptions]] A-003 (resuelta → D-033), A-004

### [2026-07-19] — Zona git de una pasada (`git-protocol.md`, T-030/T-034/T-035 completadas con reserva, D-031/D-032) y corrida 2 de T-027 (modalidad observador): FRACASADA por reaparición de L-009, T-032 cancelada

**Contexto.** Continuación directa de la sesión anterior: el siguiente paso identificado era la zona git (T-030, T-034, T-035) por ser bloqueante de la repetición de T-027.

**Zona git implementada de una pasada.** Archivo nuevo `template/_guideline/git-protocol.md`: fuente única de verdad del procedimiento git (§1 por qué el commit por etapa, §2 bootstrap automático e idempotente sin tocar remoto, §3 commit de etapa, §4 convención `tipo(<alcance>): descripción` con tabla etapa→tipo→artefacto, §5 límites — no push, no ramas, no reescribe historia). **T-030:** los 4 skills de etapa del molde (`ingest-protocol` Paso 5, `interview-protocol` Paso 3, `discovery-protocol` Paso 3, `prototype-protocol` Paso 4) ganaron un paso de commit que aplica el protocolo. **T-034:** `startup-protocol` gana Paso 4 de lectura del repositorio (`git rev-parse`, `git log --oneline -10`, `git status --short`, rama), contrastado con `progress.md`, solo-lectura estricta, divergencia se reporta no se reconcilia (NC-6); pasos renumerados. **T-035:** `closing-protocol` Paso 6.3 gana la convención de commit con criterio de elección de tipo. **G10** re-sincronizado (copias de raíz `.claude/skills/{startup,closing}-protocol/SKILL.md`). Punteros añadidos en `methodology.md` §7 y Apéndice hacia `git-protocol.md`. **Decisión D-031:** en el estadio de Prototipo el `<alcance>` del commit es literalmente `prototipo` (el Apéndice usa `<incremento>`, inexistente en este estadio). **Decisión D-032:** el procedimiento git vive como referencia única en vez de bloque duplicado por skill, para no repetir el patrón de deriva de G10.

**T-032 cancelada.** El humano canceló la conservación del fixture de reciclaje como fixture permanente versionado. Consecuencia: la reproducibilidad de la prueba queda atada a que el brief de `Pruebas_Prototipado` sobreviva intacto fuera de este repo; si se pierde o edita, los criterios de `T-027_procedimiento.md` §3 dejan de ser verificables.

**Corrida 2 de T-027 — FRACASADA por reaparición de L-009.** Ejecutada en `Pruebas_Prototipado/App_Reciclaje_2`, en terminal aparte, modalidad observador (D-029). Precondiciones verificadas: `.claude/`, `_guideline/`, `_templates/` idénticos al molde; `_persistence/` limpio; `_prototype/` inexistente; brief idéntico a la corrida 1 con sus dos trampas intactas. **NO se hizo `git init`** (precondición 5 incumplida, avisada al humano antes de arrancar). Veredicto por hallazgo (Etapa 1 — ingesta): **L-005 CERRADO** (evidencia más fuerte de lo esperado: `project.yaml` seguía declarando `name: ReciclApp` y el reader aun así escribió `Proyecto: <no declarado>`). **L-006 CERRADO** (la ambigüedad A1 cita ambos extremos §5/§8, nombra las áreas en conflicto §6↔§9, no las resuelve). **L-007 pasa técnicamente pero por el motivo equivocado** (no hubo confirmación prematura, pero nunca hubo confirmación en absoluto: el extracto quedó permanentemente `Confirmado: no` / `Estado: borrador`). **L-008 CERRADO** (§3 y §10 en `n/a` con justificación; ninguna área del cliente marcada `n/a`). **L-009 REABIERTO** (App_Reciclaje_2 nunca fue repo git, cero commits, la etapa de ingesta se cerró y avanzó a la entrevista sin commit — declara FRACASADA la corrida 2 por el §5 de `T-027_procedimiento.md`).

**Causa raíz de la reaparición de L-009 — defecto de diseño introducido esta misma sesión:** en `ingest-protocol` (y `discovery-protocol`) el commit de etapa se condicionó a la confirmación/gate humano. Cuando el humano salta el gate (instrucción legítima), el commit no ocurre nunca y nada lo recupera. Registrado como **L-013**.

**Hallazgos nuevos de la corrida (L-014, L-015, L-016).** **(1)** §7 Gatekeeper marcada `cubierta` sin método de medición (regresión frente a `parcial` en la corrida 1) — confirma empíricamente L-011/T-041, que sube de prioridad. **(2)** `interview-protocol` consume un extracto en `borrador` sin detectarlo — su Paso 0 lee la tabla de cobertura pero nunca comprueba `Estado`/`Confirmado` (verificado por grep) — **L-014**. **(3)** El interviewer atribuyó mal la procedencia de un dato: afirmó que "ReciclApp" venía del brief cuando la palabra está en `_context/project.yaml`, insumo no declarado de ninguna etapa — variante de L-005, inventar la fuente en vez del valor — **L-015**. **(4)** El criterio de aceptación de L-007 en `T-027_procedimiento.md` solo contempla "se confirmó antes de tiempo", es ciego a "no se confirmó jamás" — **L-016**.

**La corrida siguió por decisión del humano** (criterio D-029: dejar correr para producir evidencia). Etapas avanzadas: ingesta completada, entrevista en curso (6 preguntas: timebox 30min/15preguntas + prototipo 2 semanas hasta 2026-08-02 con feature freeze 2026-07-30; hipótesis de valor falsable con umbral de 70% de completitud; A1 resuelta a favor de la exclusión —sin pantalla de registro—; A2 planeación de rutas fuera del prototipo, registrada como riesgo abierto con spike de factibilidad propio; stakeholders Gerente General decide, Gerente de Operaciones veta alcance; nombre provisional ReciclApp confirmado por el humano).

**Tareas nuevas T-042…T-046.** **T-042** (alta): desacoplar el commit de etapa del gate humano en `ingest-protocol`/`discovery-protocol` — el disparador es la salida de etapa, no la aprobación; reabre parcialmente T-030. **T-043** (alta): contrato de entrada entre etapas — cada skill verifica `Estado`/`Confirmado` del artefacto que consume y avisa (no bloquea) si viene en borrador. **T-044** (alta): `project.yaml` como insumo declarado desde el Paso 0, separando metadatos de proyecto (fuente `project.yaml`) de contenido del cliente (fuente brief, `<no declarado>` si falta), todo valor con su procedencia. **T-045** (alta): checkpoints intra-etapa con commit en unidades naturales (bloque de preguntas, iteración del bucle), eager no "al suspender"; decisión pendiente sobre push registrada en **A-003**. **T-046** (media): corregir el criterio de aceptación de L-007 en `T-027_procedimiento.md`.

**Estado y siguiente paso.** T-027 sigue **en progreso**: corrida 2 fracasada por L-009 pero con L-005/L-006/L-008 cerrados con evidencia. Antes de una corrida 3 hay que aplicar T-042…T-045 y resolver la decisión pendiente sobre push en checkpoints (A-003). La precondición `git init` debe cumplirse antes de invocar al primer agente.

Ref: [[tasks]] T-030, T-032, T-034, T-035, T-041, T-042, T-043, T-044, T-045, T-046 · [[decisions]] D-031, D-032 · [[lessons]] L-013, L-014, L-015, L-016 · [[assumptions]] A-003

### [2026-07-19] — Auditoría documentación↔ejecución (T-031 completada) y sus correcciones: tres arreglos de `ingest-protocol` (T-028), pasada de consistencia entre áreas (T-029), procedimiento de repetición de T-027 con criterios previos (T-033, D-030); ocho tareas nuevas T-034…T-041, tres lecciones nuevas L-010…L-012

**Contexto.** Sesión de continuación directa de la anterior (T-027 quedó en progreso, con la Etapa 1 de ingesta evaluada y seis tareas T-028…T-033 abiertas). Esta sesión atacó primero la auditoría estructural (T-031) y luego aplicó sus correcciones más urgentes.

**T-031 completada — Auditoría de cobertura documentación↔ejecución.** Se recorrieron las conductas operativas de `principles.md` (E1–E13, NC) y `methodology.md` (§4–§7, §10, Apéndice) contra los 7 `SKILL.md` del molde. De ~20 conductas aplicables: ~10 implementadas, ~5 parciales, ~5 ausentes. **La divergencia no es aleatoria: se concentra en tres zonas.** **(1) Zona git:** G1 (no se lee `git log` al iniciar, pese a E1/E10), G5 (la convención de commits del Apéndice no la aplica `closing-protocol`), más L-009 ya conocida — el harness ni lee git al abrir ni escribe git al avanzar, solo implementó la mitad de escritura y solo al cierre. **(2) Zona fronteras de estadio:** G4 (el gate de madurez Prototipo→MVP se detecta conceptualmente pero ningún skill lo conduce: el estadio sabe empezar y no sabe terminar), G7 (timebox exigido por §4.3 como "duración tope acordada" pero nadie lo fijaba). **(3) Zona sesión principal:** G6 (E12 —guardar el plan en memoria antes de crear subagentes— no está en `AGENTS.md` ni en ningún skill), G2 (E10 incompleto: falta verificar ambiente + prueba de sanidad), G9 (motor de traza + checker de conformidad de E13/§10 no existe: los checks ya están escritos en los prompts pero nadie los ejecuta). **Sueltos:** G3 (`ingest-protocol` "escribía a partir de" `document_extract_temp.md` en vez de copiarla, incumpliendo §5.1 paso 2), G8 (reanudación E5 solo en el interviewer; el `prototype-builder` no deja checkpoint), G10 (deriva raíz↔molde: `startup-protocol` en `.claude/` raíz estaba desactualizado respecto al molde `template/`).

**Correcciones aplicadas en la misma sesión.** **G10** corregido: se sincronizó `.claude/skills/startup-protocol/SKILL.md` (raíz) desde `template/` (le faltaba el Paso 4 completo de detección de estadio). **G3** corregido: `ingest-protocol` ahora COPIA `document_extract_temp.md` en vez de "escribir a partir de", más regla invariante. **T-028 completada:** tres arreglos en `ingest-protocol` — (a) L-005: regla anti-fabricación extendida a **todo** campo de salida incluido Meta, con `<no declarado>` como valor por defecto; (b) L-007: `Confirmado por el humano` nace en `no`/`borrador` y solo pasa a `sí`/`cerrado` en escritura **posterior y separada**, la plantilla trae `no` por defecto; (c) L-008: cuarto estado de cobertura `n/a` con guarda anti-atajo (§3 canónico, §10 condicional, el resto exige justificación; ante duda gana `ausente`). **T-029 completada:** nuevo Paso 3 en `ingest-protocol` — pasada de consistencia **entre** áreas con tabla de cruces §6↔§9, §5↔§6, §7↔§2 como piso; las contradicciones se registran citando ambos extremos, sin resolverlas. Pasos renumerados. **G7 corregido:** timebox acordado en el Paso 0 de `interview-protocol` + campo nuevo *Timebox acordado* en `interview_temp.md`; no se renegocia al reanudar. **Residual detectado y corregido en la misma sesión:** `prototype-protocol` decía "cerrar al agotar el timebox" pero su Paso 0 no cargaba §8 — debía respetar un tope que nunca leía.

**Propagación.** Los cambios se propagaron a `interview-protocol`, `discovery-protocol`, `prototype-protocol`, las plantillas `document_extract_temp.md` e `interview_temp.md`, `methodology.md` §5, y los 4 prompts de agente (`onboarding-reader`, `onboarding-interviewer`, `onboarding-writer`, `prototype-builder`), cuyas tablas de conformidad contradecían los skills nuevos. Se añadieron checks nuevos que convierten los hallazgos en condiciones verificables: R8–R10/E5–E6 en el reader, I12 en el interviewer, T6–T7 en el writer, P9 en el builder — **ninguno de ellos se ejecuta todavía** (viven en G9/T-039).

**T-033 completada.** Procedimiento de repetición de T-027 escrito en `Base_Harness/T-027_procedimiento.md`: orden de ajustes, preparación de la corrida, criterios de aceptación pasa/falla por hallazgo (L-005…L-009 + G7) y criterios de la corrida completa. **Decisión D-030:** los criterios se escriben ANTES de correr la prueba (conforme a §3/P5), no se juzgan a ojo como en la corrida 1.

**Hallazgos nuevos (lecciones L-010…L-012).** **L-010:** al mover el timebox a la cabecera Meta del log se habría roto la detección del writer (ya no está en el registro de Q&A) — detectado y corregido en la misma sesión; es el mismo patrón que T-031 audita, autogenerado al editar una capa sin recorrer las demás. **L-011 (sin corregir, para T-041):** `ingest-protocol` marca §7 `cubierta` con métrica+umbral, pero `discovery-protocol` Paso 1.6 exige métrica+umbral+**método**; misma clase que L-008, entre dos skills. **L-012:** "Recicladora Oriente Verde" aparece literal en el brief pero es el nombre de la **empresa**, no del proyecto; un reader podría cumplir la regla anti-fabricación al pie de la letra y aun así fallar su intención — documentado como "aprobado con reparo" en `T-027_procedimiento.md`, sin inventar regla nueva sin evidencia (NC-1). **Confirmado empíricamente:** el proyecto de prueba `Pruebas_Prototipado/App_Reciclaje` no es repo git y su `.claude/` es anterior a todos los cambios de hoy.

**Estado de tareas.** Completadas esta sesión: T-028, T-029, T-031, T-033. T-027 sigue en progreso (Etapa 1 evaluada; la repetición espera a T-030). T-032 sigue pendiente (fixture versionado — el humano completa los archivos del proyecto de prueba), igual que T-023 y T-010.

**Tareas nuevas T-034…T-041** (una por cada gap suelto de la auditoría, más T-041 por L-011): T-034 (G1, alta), T-035 (G5, alta), T-036 (G4, media), T-037 (G6, media), T-038 (G8, media), T-039 (G9, media), T-040 (G2, baja), T-041 (alineación de "cubierta" para §7, media).

**Estado y siguiente paso.** **El siguiente paso son T-030, T-034 (G1) y T-035 (G5) — la zona git, de una pasada — y son bloqueantes de la repetición de T-027**: sin bootstrap de repo (T-030), sin lectura de `git log` al iniciar (T-034) y sin convención de commit (T-035), la próxima corrida de T-027 repetiría L-009. Después de cerrar la zona git, repetir T-027 siguiendo `T-027_procedimiento.md` (T-033) y verificar los criterios ya escritos contra L-005…L-009 + G7.

Ref: [[tasks]] T-028, T-029, T-030, T-031, T-033, T-034, T-035, T-036, T-037, T-038, T-039, T-040, T-041 · [[decisions]] D-030 · [[lessons]] L-010, L-011, L-012

### [2026-07-19] — Prueba end-to-end T-027 (modalidad observador): Etapa 1 (ingesta) evaluada contra `ingest-protocol`, 5 hallazgos registrados (L-005…L-009), modalidad observador y no-corrección del fixture (D-029), tareas T-028…T-033 abiertas

**Contexto.** Sesión dedicada a ejecutar T-027 (prueba end-to-end del flujo completo de prototipado, caso de reciclaje "ReciclApp"). **Modalidad observador (D-029):** la prueba corre en una terminal aparte, en un proyecto separado (`Pruebas_Prototipado/App_Reciclaje`); esta sesión del harness **no ejecutó** la prueba, actuó como validador, contrastando el comportamiento reportado contra el contrato escrito de cada skill.

**Etapa 1 (ingesta) evaluada.** Se revisó `document_extract.md` contra `client_brief.md`. **Conforme:** citas textuales fieles con elisiones `[...]` marcadas; 5 ambigüedades registradas sin resolver (A4 destaca: cruza bien la §10 "Dudas que tengo yo" del brief con el flujo de confirmación); fuera-de-alcance declarado conscientemente; §7 marcada `parcial` pese a tener métrica+umbral (el sesgo hacia el hueco funcionando tal como se diseñó); §5 correctamente `cubierta` (una sospecha inicial del validador de que faltaba identificar al generador se retiró tras verificar que el brief sí lo cubre).

**Hallazgos (registrados como L-005…L-009 en `lessons.md`):**
- **L-005:** el reader FABRICÓ el nombre del proyecto ("ReciclApp" no aparece en el brief; el título conservaba el marcador `<nombre-del-proyecto>` sin rellenar). La regla del skill prohíbe *citar* marcadores, pero no impide *rellenarlos* con una invención plausible.
- **L-006 (el más caro):** contradicción interna del brief no detectada — su §5 pone la validación de correo en el camino feliz y su §8 la excluye explícitamente. El reader extrajo ambas citas correctamente pero nunca las cruzó: el bucle de extracción por área es ciego a contradicciones transversales.
- **L-007:** escribió `Confirmado por el humano: sí` / `Estado: cerrado` antes de pedir la confirmación al humano.
- **L-008:** §3 y §10 quedaron marcadas `ausente` por falta de un estado `n/a`; `interview-protocol` solo lee el estado de cobertura (regla: *ausente → preguntar el área completa*), así que §3 —que por diseño deduce el Descubridor, no el cliente— entrará indebidamente en la agenda de entrevista.
- **L-009:** no hubo commit ni push durante la prueba. Verificado: el proyecto de prueba no es un repo git, y de los siete skills del molde solo `closing-protocol` toca git. "Commit por etapa" está documentado en `methodology.md:515` y `principles.md:37`, pero ningún skill de etapa lo implementa — el agente no incumplió su propio skill.

**Observación estructural.** Tres de las cinco lecciones (L-004 previa, L-008, L-009) son el **mismo defecto**: una regla escrita en una capa (documentación/metodología) que la capa operativa (el `SKILL.md` que ejecuta) no implementa. La divergencia documentación↔ejecución parece sistemática, no incidental — de ahí T-031 (auditoría transversal) en vez de seguir parcheando skills uno a uno.

**Decisiones de conducción (D-029).** El humano decidió explícitamente: **(1)** no corregir nada en el proyecto de prueba ReciclApp — queda intacto con sus defectos; **(2)** dejar correr §3/§10 sin corregir a propósito, para que la entrevista produzca evidencia observable del gap de L-008; **(3)** todos los ajustes se planifican como tareas en `Base_Harness` (T-028…T-033), para aplicarlos en la próxima sesión y repetir la prueba desde cero.

**Estado y siguiente paso.** T-027 queda **en progreso**, con la Etapa 1 evaluada. Próxima sesión: aplicar T-028 (tres arreglos de `ingest-protocol`), T-029 (pasada de consistencia entre áreas) y T-030 (commit por etapa + bootstrap de repo) antes de repetir la prueba siguiendo el procedimiento de T-033. T-031 (auditoría documentación↔ejecución) y T-032 (fijar la ubicación del fixture de reciclaje) quedan abiertas, sin bloquear la repetición inmediata.

Ref: [[tasks]] T-027, T-028, T-029, T-030, T-031, T-032, T-033 · [[decisions]] D-029 · [[lessons]] L-005, L-006, L-007, L-008, L-009

### [2026-07-19] — Condición de entrada al estadio de Prototipo: ruta canónica `_prototype/` (D-028), detección de estadio en `startup-protocol` (Paso 4, propone-no-ejecuta), sección "Arranque de proyecto" en `AGENTS.md`, plantilla `client_brief_temp.md`, y corrección de dos gates documentados-pero-no-implementados (L-004)

**Contexto.** Se arrancó con la intención de ejecutar T-023 (ampliar el inventario de `register-harness`), pero el usuario planteó una duda que destapó un hueco mayor: *"si completo `_context/` incluyendo `client_brief.md`, ¿qué le digo a Claude Code? ¿cómo sabe que estamos iniciando esta fase?"*. Se decidió con el usuario atender ese hueco primero. **T-023 no se tocó y sigue pendiente.**

**Hueco detectado.** No existía condición de entrada al estadio de Prototipo. `AGENTS.md` no mencionaba `client_brief.*`; el encadenamiento reader→interviewer→writer→builder solo vivía como prosa en `methodology.md` §5; nada le decía a la sesión principal cómo detectar que un proyecto está arrancando ese estadio.

**Ruta canónica `_prototype/` (D-028).** Los 4 skills del estadio y 3 plantillas referenciaban "la carpeta del estadio de prototipo" / `<estadio-prototipo>` **sin definirla nunca**; `prototype-protocol` incluso decía "la numeración de carpeta es convención del proyecto, no obligación". Sin ruta fija, la autodetección era imposible. El usuario eligió `_prototype/` (hermana de `_context/`, `_guideline/`, `_persistence/`, `_templates/`), conteniendo `document_extract.md`, `interview_document.md`, `discovery.md` y `prototype/`. Se alineó en 12 archivos.

**Detección de estadio en `startup-protocol`.** Nuevo "Paso 4 — Detectar el estadio del proyecto": tabla de 6 filas por presencia de artefactos, se aplica la primera fila que matchea (con nota de que si existe `_increments/` manda `state.yaml` §7.1, no esta tabla). El antiguo Paso 4 pasó a ser Paso 5 e informa el estadio detectado, proponiendo el siguiente paso nombrando el agente a invocar. Regla añadida: **detectar y proponer, NUNCA ejecutar** (NC-6). Se actualizó su `description` de frontmatter, las reglas invariantes, y el agente `sesion-starter.md` (cuerpo, principios y `description`).

**Sección "Arranque de proyecto (estadio de Prototipo)" en `AGENTS.md`.** Hermana de "Protocolos de sesión": tabla de los 4 agentes (orden, skill, artefacto, cuándo), condición de entrada y delegación por herramienta. Se documentó también `client_brief.*` en la sección `_context/`.

**Plantilla nueva `_templates/client_brief_temp.md`.** Documento del cliente, escrito en lenguaje de cliente (no jerga de la metodología), **parcial por diseño**: dejar una sección vacía es respuesta válida, lo que falte se cubre en la entrevista (para no reintroducir parálisis por diseño). 10 secciones con mapeo en comentario a las áreas §1–§10 del discovery, para uso del `onboarding-reader`. Referenciada desde `AGENTS.md` e `ingest-protocol`.

**Lección importante (L-004).** Se detectó una inconsistencia introducida en esta misma sesión: se escribió en `AGENTS.md` que "entre el paso 3 y el 4 hay un gate humano", y al verificar el skill correspondiente resultó **falso** — `discovery-protocol` decía "marcar el entregable como cerrado" sin pedir aprobación explícita. Documentar un gate en `AGENTS.md` no lo implementa; hay que verificar el `SKILL.md` que lo ejecuta. Se corrigió `discovery-protocol` (su Paso 2.3 ahora pide aprobación explícita, queda en borrador hasta obtenerla, + regla invariante nueva). Se encontró y corrigió el mismo defecto en `interview-protocol`: cerraba la entrevista por su cuenta; ahora la propone y espera el OK del humano.

**Otro ajuste.** Se añadió a `ingest-protocol`: un `<marcador>` de plantilla sin rellenar es área **AUSENTE**, no contenido (citarlo sería fabricar evidencia); y el brief se mapea por contenido, no por número de sección.

**Gates del estadio (quedan 4 explícitos):** ① qué archivo ingerir + cierre del extracto (reader), ② cierre de entrevista (interviewer), ③ cierre del discovery (writer) — el más importante, único insumo del prototipo, ④ gate de madurez Prototipo→MVP que el builder no cruza.

**Estado y siguiente paso.** Todo el trabajo se hizo dentro de `template/` (deliverable-only, D-022); no se tocó la raíz `.claude/`. **Próxima sesión (decidido por el usuario): prueba end-to-end del flujo completo de prototipado**, encadenando T-024 (probar `prototype-builder` con discovery real) y T-026 (probar la ruta documental con `client_brief` real) — evaluar si conviene fusionarlas, referenciarlas juntas, o abrir una tarea nueva que las englobe. T-023 sigue pendiente y sigue creciendo (el re-sync a opencode/Gemini debe cubrir también estos cambios de detección de estadio).

Ref: [[tasks]] T-024, T-026, T-023 · [[decisions]] D-028 · [[lessons]] L-004

### [2026-07-19] — Ruta documental del Descubridor: tercer agente `onboarding-reader` + skill `ingest-protocol`, entrevista solo-huecos y writer con dos insumos (D-027) — T-025 completada

**Contexto.** El usuario planteó un caso real recurrente: el cliente **entrega un documento** describiendo la aplicación que quiere. El flujo saltaba directo al `onboarding-interviewer`, que entrevistaba las diez áreas aunque el cliente ya las hubiera redactado — quemando el timebox de §4.3 y comunicándole al cliente que no habían leído su documento. Petición: verificar primero si el documento existe y, si existe, preguntar **solo lo que falta**; si no, conservar el flujo actual.

**Diseño (D-027).** El arquetipo Descubridor pasa de **dos a tres agentes**, separando *ingerir* / *elicitar* / *estructurar* (E4), con la ruta documental como **camino condicional**:

```
_context/client_brief.*  →  onboarding-reader  →  document_extract.md  ─┐
                                                          ↓             │
                                            onboarding-interviewer      │
                                        (solo huecos + ambigüedades)    │
                                                          ↓             │
                                              interview_document.md  ───┤
                                                                        ↓
                                                          onboarding-writer  →  discovery.md
```

Sin `client_brief.*` el reader no escribe nada y el flujo cae a la entrevista completa: **la ausencia de documento es el caso normal, no un error**.

**Decisiones de diseño relevantes** (varias tomadas por el usuario contra mi recomendación inicial, y mejor así):
- **Tercer agente en vez de un paso 0.5 dentro de `interview-protocol`.** Yo recomendé el paso interno por E4; el usuario prefirió la frontera limpia *lectura ≠ elicitación*, con `document_extract.md` como **artefacto de primera clase**. Efecto lateral positivo: el check **I4 Single Writer** del interviewer queda **intacto** (cada artefacto conserva un solo autor), cosa que la alternativa de sembrar el log habría roto.
- **El writer pasa a dos insumos** en vez de duplicar el extracto dentro del log. El material del documento **no se copia**: vive en el extracto y el writer lee ambos. Por eso leer el extracto es **obligatorio** (check W0) — ignorarlo produce un `discovery.md` mutilado justo en lo que el cliente se molestó en escribir.
- **Sesgo deliberado hacia el hueco:** ante la duda el reader marca `parcial`/`ausente`, nunca `cubierta`. Repreguntar de más cuesta una pregunta; dar por cubierta un área que no lo está mete un **hueco silencioso** que nadie ve hasta el prototipo.
- **El reader no resuelve ambigüedades:** lo contradictorio o vago del brief se registra para que **lo pregunte** el interviewer (NC-1).
- **Precedencia ante conflicto:** manda la entrevista (es posterior y el humano hablaba conociendo su documento), pero la discrepancia se **declara** en el `discovery.md`, no se silencia.

**Qué se creó.** `template/.claude/agents/onboarding-reader.md` (perfil de conformidad R1–R7 + oráculo brief→extract E1–E4 + política de evaluación), `template/.claude/skills/ingest-protocol/SKILL.md` (localizar con confirmación → extraer por áreas → confirmación por bloque) y `template/_templates/document_extract_temp.md` (Meta / Cobertura / Extractos citados / Ambigüedades / Fuera de alcance).

**Qué se modificó.** `interview-protocol` e `onboarding-interviewer` (agenda fijada por la tabla de cobertura, prohibición de duplicar el extracto, redefinición de *hueco*, checks nuevos **I8–I11**); `discovery-protocol` e `onboarding-writer` (dos insumos, regla de precedencia, check **W0**, universo de traza ampliado a **log ∪ extracto** y nuevo **T5**); `_templates/interview_temp.md` (Meta con *Extracto documental* y *Áreas cubiertas*, aviso sobre huecos); `methodology.md` §5 (Descubridor = tres agentes + flujo del estadio); `prototype-builder` (P7 incluye el nuevo artefacto).

**Estado y siguiente paso.** T-025 completada. **T-023 crece**: el re-sync a opencode/Gemini debe incluir `onboarding-reader` + `ingest-protocol`, y su inventario hardcodeado hoy solo cubre los agentes de sesión — hay que ampliarlo, no solo re-ejecutarlo. Nueva **T-026**: probar la ruta documental end-to-end con un `client_brief` real, encadenable con T-024 sobre el mismo caso de reciclaje (brief → discovery → prototipo). Todo el trabajo es **deliverable-only** en `template/.claude` (D-022): la raíz `.claude/` no se tocó.

Ref: [[tasks]] T-025, T-026, T-023 · [[decisions]] D-027

### [2026-07-18] — Prototipado Parte 2 (Prototipador): reubicación de la frontera humano↔agente (D-025) y materialización de `prototype-builder` + `prototype-protocol` con observabilidad §10 (D-026) — T-022 completada
- **Estado:** completado
- **Resumen:** Sesión de dos frentes, continuación directa de T-021/T-022 (materializar el arquetipo Prototipador). **(1) Cambio de canon (D-025):** se reubicó la frontera humano↔agente del prototipo (`template/_guideline/methodology.md` §4.2) de *deseabilidad/factibilidad* a **juicio vs. materialización**: el agente Prototipador materializa TAMBIÉN deseabilidad (wireframes/mockups/HTML clicable), no solo factibilidad; el humano conserva el juicio de UX y el mago de Oz. Se añadió la "regla de medio" (el prototipo se construye en la tecnología más barata que valide la hipótesis; el medio del prototipo no tiene por qué ser el del producto final). Se introdujo el concepto de **medio/canal por actor** (no por proyecto, porque "ambos" nunca se construye a la vez): nuevo campo en §6 de `template/_templates/discovery_temp.md`, con su elicitación reflejada en `template/.claude/skills/interview-protocol/SKILL.md` y `template/.claude/skills/discovery-protocol/SKILL.md`, y en el mapa de áreas de `template/_templates/interview_temp.md`. Se actualizó también la nota del Prototipador en §5 de `methodology.md`. **(2) Materialización del Prototipador (D-026):** se creó el agente `template/.claude/agents/prototype-builder.md` + skill `template/.claude/skills/prototype-protocol/SKILL.md` (deliverable-only, coherente con `D-022`). Es un solo agente, autónomo pero agéntico (insumo = `discovery.md`; construye en bucle escribir→ejecutar→ajustar, por eso lleva `Bash`). Ramifica por §3 del discovery (mockup de deseabilidad / spike de factibilidad) y construye solo el camino feliz del generador en el medio de ese actor. Convención de artefacto: `<estadio-prototipo>/prototype/`. **(3) Observabilidad/evaluación del `prototype-builder` (parte de D-026):** el agente incluye su perfil de conformidad §10 (checks P1–P8), un oráculo de trazabilidad discovery→prototype (T1–T2, espejo del oráculo log→discovery del writer), una nota de honestidad de que los checks P3/P4/P6 son semánticos (no diff limpio) por no tener plantilla ni tests, y su evaluación de calidad anclada al Gatekeeper (§7) cruzado por el humano, con el juez LLM diferido por E4.
- **Siguiente paso:** **(a)** Falta PROBAR `prototype-builder` end-to-end con un `discovery.md` real (usar el caso de reciclaje como fixture) — sin esa evidencia real, el motor genérico de traza/conformidad y el juez LLM calibrado siguen diferidos por E4. **(b)** Falta re-sincronizar el nuevo agente/skill (`prototype-builder`, `prototype-protocol`) y los cambios de D-025 (`discovery_temp.md`, `interview-protocol`, `discovery-protocol`, `interview_temp.md`) a opencode/Gemini vía `register-harness` (T-023, acumulada con la deuda previa del Descubridor).
- **Referencias:** `template/_guideline/methodology.md`, `template/_templates/discovery_temp.md`, `template/_templates/interview_temp.md`, `template/.claude/skills/interview-protocol/SKILL.md`, `template/.claude/skills/discovery-protocol/SKILL.md`, `template/.claude/agents/prototype-builder.md`, `template/.claude/skills/prototype-protocol/SKILL.md`, [[tasks]] T-022, T-023, [[decisions]] D-025, D-026

### [2026-07-17] — Diseño de observabilidad/evaluación del Descubridor (perfiles de conformidad §10, D-023) y promoción de A-002 → D-024 (conservar `interview_document.md` como traza)
- **Estado:** completado
- **Resumen:** Sesión de dos frentes, continuación directa de T-021. **(1) Diseño escrito de observabilidad/evaluación de los dos agentes del Descubridor** (sin construir el motor, por decisión explícita del usuario ante la disyuntiva de alcance: "solo diseño, no construir", coherente con E4/§9 de `methodology.md`): se añadió el "Perfil de conformidad (§10)" al prompt de `template/.claude/agents/onboarding-interviewer.md` (checks I1–I7 de conformidad procedimental: instanciación, append-only, persistencia inmediata, Single Writer, no-interpreta, reanudabilidad, cierre bien formado) más su política de evaluación de calidad — sin juez LLM offline, porque su insumo es un diálogo en vivo sin fixture estático; se mide por cobertura determinista del esqueleto `discovery_temp.md` (§1–§9 con ≥1 pregunta o hueco declarado) + gate humano autoritativo. Se añadió el perfil equivalente a `template/.claude/agents/onboarding-writer.md` (checks W1–W6: lectura-antes-de-escritura, instanciación, estructura intacta, Single Write, completitud, Gatekeeper medible) más un **oráculo de trazabilidad log→discovery** (checks T1–T4: actores, camino feliz, Gatekeeper y huecos declarados deben trazar al `interview_document.md` — caza invención) y una nota de candidatura a juez LLM offline, diferida. Registrado en `D-023`. **(2) Promoción de A-002 a decisión:** el usuario confirmó explícitamente CONSERVAR el log crudo `interview_document.md` como traza junto a `discovery.md` (no descartarlo tras la síntesis), motivo reforzado porque conservar el log habilita el oráculo de trazabilidad recién diseñado en `D-023`. `A-002` quedó marcado como confirmado/promovido en `assumptions.md`, y se registró la decisión `D-024` en `decisions.md` con ambos índices sincronizados. No se tocó `methodology.md` en esta sesión (el marco de §8/§10/§9 ya estaba completo desde rondas previas).
- **Siguiente paso:** Sigue pendiente T-022 (construir el agente+skill `prototipador`) y T-023 (re-sync a opencode/Gemini). Cuando haya evidencia (E4), construir el motor genérico de traza/conformidad, el dataset de fixtures con defectos sembrados y el juez LLM calibrado para `onboarding-writer` (trabajo diferido por `D-023`, sin tarea propia todavía — se retomará como parte del alcance de T-022 o de una tarea nueva cuando el Prototipador exista y aporte evidencia real de uso).
- **Referencias:** `template/.claude/agents/onboarding-interviewer.md`, `template/.claude/agents/onboarding-writer.md`, [[tasks]] T-021, T-022, [[decisions]] D-023, D-024, [[assumptions]] A-002

### [2026-07-17] — Prototipado Parte 2: plantilla `discovery_temp.md` y arquetipo Descubridor materializado como dos agentes (`onboarding-interviewer` + `onboarding-writer`) (T-021)
- **Estado:** completado
- **Resumen:** Continuación del plan de prototipado (Parte 2), sobre lo canonizado en Parte 1 (T-020). **(1) Plantilla `discovery_temp.md`:** creada en `template/_templates/discovery_temp.md`, modela la forma del "entregable de descubrimiento" con 10 secciones (Meta, Objetivo, Hipótesis de valor central, Tipo de prototipo dominante deseabilidad/factibilidad, Stakeholders, Actores con taxonomía Generador/Operador/Administrador y sus ausencias/colapsos, Camino feliz por actor con el generador obligatorio, Gatekeeper, Timebox+feature freeze, Exclusiones, Split por audiencia opcional). Estilo pedagógico con marcadores, hermana de `state_temp.yaml` (T-018). **(2) Arquetipo Descubridor materializado como DOS agentes** (decisión de diseño acordada con el usuario, separación de responsabilidades bajo E4): `onboarding-interviewer` (agente en `template/.claude/agents/`) + skill `interview-protocol` (`template/.claude/skills/`) conduce SOLO la entrevista; hace una pregunta y guarda de inmediato la Q&A en `interview_document.md` (append-only), lo que hace la entrevista **reanudable** (el log parcial es el estado si se suspende); no interpreta ni estructura. `onboarding-writer` (agente) + skill `discovery-protocol` (reescrito) es un subagente **autónomo** (insumo = archivo, no diálogo) que sintetiza `interview_document.md` en el `discovery.md` estructurado (clasifica actores, extrae camino feliz, fija Gatekeeper, declara exclusiones). Nueva plantilla `template/_templates/interview_temp.md`: log crudo append-only con pista de reanudación. **(3)** `methodology.md` §5 recibió una nota breve de "Realización concreta del Descubridor" (interviewer + writer, separar elicitar de estructurar). **(4)** Todo el trabajo se hizo SOLO en `template/.claude` (entregable); la raíz `.claude/` quedó limpia y sin tocar, por instrucción explícita del usuario: estos arquetipos de construcción del prototipado son deliverable-only, el propio proyecto harness no los usa para sí mismo. Registrado en `D-021`, `D-022`.
- **Siguiente paso:** Falta el agente+skill `prototipador` (dentro de T-021 o como tarea derivada). Falta reflejar los nuevos agentes/skills/plantillas a opencode/Gemini vía `register-harness` (re-sync pendiente, ver T-012/T-010). Consideración abierta sin decidir: si `interview_document.md` se conserva como traza junto a `discovery.md` tras la síntesis, o se descarta (ver `assumptions` A-002).
- **Referencias:** `template/_templates/discovery_temp.md`, `template/_templates/interview_temp.md`, `template/.claude/agents/onboarding-interviewer.md`, `template/.claude/agents/onboarding-writer.md`, `template/.claude/skills/interview-protocol/`, `template/.claude/skills/discovery-protocol/`, `template/_guideline/methodology.md`, [[tasks]] T-021, T-022, [[decisions]] D-021, D-022, [[assumptions]] A-002

### [2026-07-17] — Cuarta ronda de `methodology.md`: gates + TDD en `state.yaml` (T-018), metodología cableada en `AGENTS.md`, y arranque de Prototipado Parte 1 (taxonomía de actores + Descubridor)
- **Estado:** completado
- **Resumen:** Sesión de tres frentes. **(1) T-018 completada — gates + TDD en `state.yaml`:** se creó `template/_templates/state_temp.yaml`, la plantilla física del estado por incremento (§7.1): espina de 11 pasos, un modelo de gate reutilizable (`status`/`fecha`/`resoluciones`, documentado como comentario aplicable a los pasos 5, 7 y 11), el bucle TDD del paso 8 (Construir) con `cases` etiquetados por `component`/`owner`, campo `stage` (`pending|red|green|refactor|done`) y flag `caracterizacion` (test que nace verde, fija conducta ya existente), y los evaluadores transversales del paso 10 (Verificar: `spec_verifier`, `code_review`, `security_review`). Se enriqueció `§7.1` de `methodology.md` con las reglas nuevas (Construir es un bucle TDD; los gates dejan traza de resoluciones), el árbol de `_increments/<id>/` y la referencia a la plantilla. **(2) Metodología huérfana resuelta:** se detectó que `template/AGENTS.md` solo referenciaba `principles.md`, no `methodology.md` — ningún proyecto instanciado sabía que la metodología existía. Se añadió una sección "Metodología de construcción" en `AGENTS.md`, hermana de la de `principles.md`. Decisión de diseño: la referencia va en `AGENTS.md` (SSOT), no en `CLAUDE.md`, para no romper la regla de punteros sin duplicar procedimientos (D-013). **(3) Trabajo NUEVO iniciado — Prototipado Parte 1 (modo DEFINICIÓN, no construcción):** siguiendo el plan acordado de 3 partes (método → descubridor → prototipador), se canonizó la Parte 1 en `methodology.md`. `§4.3` (nueva): taxonomía de actores por defecto GENERADOR → OPERADOR → ADMINISTRADOR como lente de elicitación (piso, no techo; roles pueden faltar o colapsar; prioridad no lineal — el generador es obligatorio para arrancar, operador/administrador bajo demanda; el generador basta para avanzar un MVP, el resto se simula con mago de Oz bajo E4). `§5`: nuevo arquetipo **Descubridor** (conduce la entrevista con el humano, alcance acotado para evitar parálisis, produce un entregable de descubrimiento con Gatekeeper propio, observable+evaluable, insumo del Prototipador). Se conectó con el Prototipador ya existente (consume el entregable de descubrimiento, prototipa el camino feliz empezando por el generador); orden fijado: Descubridor → Prototipador. `§4.2` ajustada: la frontera humano↔agente ahora aclara que el descubrimiento lo conduce un agente (el Descubridor), mientras que la materialización de mockups sigue siendo humana. Registrado en `D-018`, `D-019`, `D-020`.
- **Siguiente paso:** Continuar el plan de prototipado en su Parte 2: definir y luego construir los agentes+skills `descubridor` y `prototipador` (agnósticos, patrón wrapper+skill). Primer paso de definición pendiente: fijar los campos concretos del "entregable de descubrimiento" (actores + camino feliz por actor + stakeholders + Gatekeeper + exclusiones) y crear la plantilla `_templates/discovery_temp.md`. La Parte 3 (perfil de conformidad §10 + rúbrica §8 por agente, motor genérico de traza/conformidad y juez LLM) se construye una sola vez, como tarea aparte posterior a la Parte 2. Sigue pendiente T-012, T-003, T-010 (Codex), T-004. Nota: el árbol "Estructura del entregable" del `CLAUDE.md` raíz quedó desactualizado a propósito (el usuario pidió no tocarlo por ahora).
- **Referencias:** `template/_templates/state_temp.yaml`, `template/_guideline/methodology.md`, `template/AGENTS.md`, [[tasks]] T-018, T-019, T-020, [[decisions]] D-018, D-019, D-020

### [2026-07-17] — Tercera ronda de `methodology.md`: §7.1 estado por incremento (`state.yaml`) y §5.2 hermano de seguridad (T-017)
- **Estado:** completado
- **Resumen:** Continuación de T-017, arrancando desde una muestra `state.json` de un flujo previo del usuario (`temp.md`, sin versionar como fuente). **(1) §7 enriquecida + §7.1 nueva (Estado por incremento `state.yaml`):** se formalizó el modelo de persistencia por vertical slice. Dos capas de persistencia con naturaleza distinta: `_persistence/` es narrativa (Markdown, bitácora) y `state.yaml` de la slice es estructurado (máquina de estado del ciclo de vida §3, consumida por el orquestador y por checks de conformidad §10). Reglas del modelo: **espina única de 11 pasos** — el ciclo NO se bifurca por capa técnica; frontend/backend/DB no son pasos sino *tareas etiquetadas* (`component` + `owner`) dentro de Construir (paso 8); si algo es valor end-to-end independiente, es OTRA slice (otro `state.yaml`). Las **revisiones transversales** (calidad de código §5.2, seguridad) son entradas de evaluación en Verificar (paso 10), no pasos nuevos; la seguridad-*comportamiento* es criterio de aceptación en la spec. **Single Writer:** el orquestador (sesión principal) es el escritor único de `state.yaml`; cada artefacto lo escribe solo su agente productor. `state.yaml` vive en la rama de la slice y se archiva al integrar (mecanismo de reanudación). Se incluyó un sketch YAML compacto con ejemplo de `construir` (cases etiquetados por `component`/`owner`) y `verificar` (evaluadores `spec_verifier`/`code_review`/`security_review`). **(2) §5.2 nota "Hermano de seguridad":** `security-reviewer` como evaluador transversal análogo al Revisor de código (mismas reglas: independiente, contexto fresco, hallazgos → tests que fallan, adoptar por E4), distinguiendo seguridad-revisión de seguridad-comportamiento. **(3)** Tabla de contenidos actualizada con la fila 7.1. Nota: el modelo simplifica el `state.json` previo del usuario eliminando el stage `notebook_writer` (coherente con D-010, un solo prototipado al inicio). Registrado en `D-014`, `D-015`, `D-016`, `D-017`.
- **Siguiente paso:** Seguir trabajando en `state.yaml`: modelar los **gates con sus resoluciones** (como en la muestra `temp.md`: `gate_paso_5/7/9` con `status`, `fecha` y lista de `resoluciones`) y el modelado de los **casos TDD** dentro de Construir. Relacionar con T-018 (estructura física de carpetas / `_template/`), donde se aterrizará la ruta real de `_increments/<id>/`.
- **Referencias:** `template/_guideline/methodology.md`, [[tasks]] T-017, T-018, [[decisions]] D-014, D-015, D-016, D-017

### [2026-07-17] — Segunda ronda de `methodology.md` (elimina prototipado por-incremento, observabilidad/evaluación, Revisor de código) y simplificación de `AGENTS.md` (T-016)
- **Estado:** completado
- **Resumen:** Sesión continuando la iteración de `template/_guideline/methodology.md` (T-016) más una simplificación de `template/AGENTS.md`. **(1) Limpieza del prototipado:** se eliminó "Prototipar" como fase repetida del ciclo de incremento (el término se usaba en dos sentidos confusos: estadio inicial y paso por-slice). Ahora el prototipo es UN SOLO estadio de alto nivel al inicio del proyecto; las vertical slices son funcionalidad real, no prototipos. Cambios: §2 (espina común) pasó a `Definir → Especificar → Planear → Construir → Verificar → Integrar`; §3 (ciclo de incremento) pasó de 13 a 11 pasos; se añadió nota de "spikes de excepción" (prototipado técnico puntual, opcional, distinto del estadio Prototipo); §5 reubicó al *Prototipador* al estadio inicial; §6 ajustó sus gates al nuevo ciclo. Registrado en `D-010`. **(2) §5.1 Contrato de trabajo de un constructor de entregables:** formaliza el patrón plantilla (`_template/`) → instanciar/copiar → rellenar → reportar → gate humano, con Single Writer y observabilidad/evaluación. **(3) §10.1 Flujo end-to-end de observabilidad** (diagrama de secuencia, ejemplo Definidor): traza + conformidad deterministas (sin LLM), juez de calidad LLM aparte/offline. **(4) §8.1 Flujo de evaluación** (diagrama, ejemplo Definidor): tres capas — check determinista de completitud/trazabilidad → gate humano → juez LLM calibrado offline. **(5) §3.1 Observabilidad y evaluación del ciclo TDD (código):** el test como contrato de forma + evidencia de conformidad (RED-antes-de-GREEN) + oráculo de evaluación; mutation testing como validación de los propios tests. **(6) §5.2 Revisor de código:** nuevo arquetipo evaluador independiente (análogo al juez LLM, pero para código), distinto del Verificador, adoptado bajo E4. Registrado en `D-011`. **(7) §5.3 Especialización de flota** (ejemplo frontend/backend): se aclaró que es una decisión de instanciación de cada proyecto, no de arquetipo del molde agnóstico. Registrado en `D-012`. **(8)** Se añadió una tabla de contenidos a `methodology.md` (navegación por número de sección, sin anclas por caracteres frágiles) + nota de "gatillo de división" para el futuro (si el bloque agentes/evaluación/observabilidad supera ~250 líneas, dividir en `methodology.md` + `agents-and-evaluation.md`). Registrado como supuesto `A-001`. **(9) Simplificación de `template/AGENTS.md`:** de 273 a 101 líneas. Se eliminó la duplicación de los protocolos de inicio/cierre y las tablas de portabilidad opencode/Gemini, que ya viven de forma canónica en los `SKILL.md` correspondientes; `AGENTS.md` ahora solo apunta a ellos. Se añadió una "Regla de diseño" en el header del archivo (marco mínimo + punteros, no duplicar procedimientos). Registrado en `D-013`.
- **Siguiente paso:** Continuar iterando `methodology.md` si surgen más secciones a definir (estructura física de carpetas para artefactos por incremento y `_template/` sigue pendiente de instanciación, ver `D-008`). Vigilar el tamaño del bloque agentes/evaluación/observabilidad frente al gatillo de división (`A-001`). Sigue pendiente T-010 (Codex), T-012 (re-sync `proyecto_prueba` en opencode) y T-013 (provisionar `.gemini/` real).
- **Referencias:** `template/_guideline/methodology.md`, `template/AGENTS.md`, [[tasks]] T-016, T-017, [[decisions]] D-010, D-011, D-012, D-013, [[assumptions]] A-001

### [2026-07-17] — `principles.md` cableado como comportamiento vinculante y creación de `methodology.md` agnóstico (T-014, T-015)
- **Estado:** completado
- **Resumen:** Sesión de dos frentes sobre el guideline del harness (`template/_guideline/`). **(1) `principles.md` como fuente canónica de comportamiento vinculante:** se auditó `template/_guideline/principles.md` y se confirmó TOTALMENTE AGNÓSTICO (P1–P8, E1–E13, NC-1…NC-6, sin referencias a dominio/lenguaje/stack), por lo que no requirió cambios en el propio archivo. Se actualizaron en cambio los archivos de INSTRUCCIONES para que todo agente lo siga: `template/AGENTS.md` (nueva sección "Principios de comportamiento (VINCULANTE)" apuntando a `_guideline/principles.md`, inmutable y prevalente), `CLAUDE.md` de la raíz (sección vinculante equivalente), `.claude/agents/sesion-starter.md` y `sesion-closer.md` (+ espejos en `template/.claude/agents/`, bullet "Comportamiento vinculante" con énfasis en NC-1/NC-6), y `.claude/skills/register-harness/SKILL.md` (+ espejo en template) junto con la sección Portabilidad de `template/AGENTS.md` (se añadió `_guideline/principles.md` a la fuente única de verdad/baseline que se propaga a opencode/Gemini). Se decidió referenciar el guideline por CONVENCIÓN (`_guideline/`), igual que la persistencia, sin crear una carpeta de instancia `905_guideline/` (fuera de alcance). **(2) `methodology.md` agnóstico creado desde cero** en `template/_guideline/methodology.md`, reemplazando la versión previa ligada a ZeroLeak. Cubre dos tipos de proyecto (Software y Ciencia de datos/ML) con espina común, dos ejes (MADUREZ: Prototipo→MVP→Evolucionado; INCREMENTO: slices verticales), ciclo de vida de 13 pasos con gates humanos, arquetipos de agente (constructores de ENTREGABLES vs de CÓDIGO, no una flota concreta), gates, persistencia, evaluación dimensionada, evolución (E4) y observabilidad (E13). La estructura física de carpetas para artefactos por incremento y `_template/` queda PENDIENTE DE INSTANCIACIÓN (no se inventó numeración de carpetas). **(3)** Partiendo de un archivo temporal `temp.md` (no versionado como fuente, lo borra el usuario), se reescribió la §4 de `methodology.md` (Estadios de madurez) en 4.1–4.5: dos tipos de prototipo (DESEABILIDAD vs FACTIBILIDAD), frontera humano↔agente (deseabilidad = actividad humana/diseño; agentes desde factibilidad), disciplina de alcance del prototipo (timebox + feature freeze, camino feliz, roles/actores priorizados, split prototyping, gatekeeper cuantitativo, exclusiones explícitas) y frontera dura Prototipo→MVP (MVP = primer entregable FUNCIONAL, Tracer Bullet). Ajustes menores en §1 y §2. Registrado en `D-007`, `D-008` y `D-009`.
- **Siguiente paso:** Continuar trabajando sobre `methodology.md` en la próxima sesión (siguiente iteración del documento). Sigue pendiente T-010 (Codex), T-012 (re-sync `proyecto_prueba` en opencode) y T-013 (provisionar `.gemini/` real).
- **Referencias:** `template/_guideline/principles.md`, `template/_guideline/methodology.md`, `template/AGENTS.md`, `CLAUDE.md`, `.claude/agents/sesion-starter.md`, `.claude/agents/sesion-closer.md`, `.claude/skills/register-harness/SKILL.md`, [[tasks]] T-014, T-015, [[decisions]] D-007, D-008, D-009

### [2026-07-17] — Regla de protocolos vía agente y soporte de Gemini en `register-harness` (T-011)
- **Estado:** completado
- **Resumen:** Dos frentes. **(1) Regla de trabajo nueva (feedback del usuario):** los protocolos de sesión deben ejecutarse SIEMPRE delegando en su agente especializado, nunca directamente desde la sesión principal — inicio → agente `sesion-starter`, cierre → agente `sesion-closer`. Se actualizó `CLAUDE.md` de la raíz (secciones "Inicio de sesión" y "Cierre de sesión") para hacerlo obligatorio vía agente, no solo vía skill. Registrado como `D-006` y `C-002`. **(2) T-011 completada + avance de T-010 (Gemini):** se verificó con documentación oficial de Gemini CLI (vía ctx7 + web) que Gemini tiene skills y subagentes NATIVOS con rutas de proyecto análogas a opencode: skills en `.gemini/skills/<n>/SKILL.md` (copia directa) y agentes en `.gemini/agents/<n>.md` (traducción de frontmatter). Se extendió `.claude/skills/register-harness/SKILL.md` con un nuevo "Paso 4 — Destino: Gemini CLI" (auditar + provisionar): tablas de ubicaciones, traducción de frontmatter Claude→Gemini (`name` SE CONSERVA, se añade `kind: local`, se elimina `color`, `tools` como lista YAML de nombres nativos: Read→read_file, Write→write_file, Edit→replace, Glob→glob, Grep→grep_search, Bash→run_shell_command, Skill→activate_skill), tabla de modelos destino (`sesion-starter → gemini-3-flash`, `sesion-closer → gemini-3-pro`) y los dos agentes de ejemplo traducidos. Se actualizaron el frontmatter/descripción del skill y el Paso 0 (herramientas soportadas: opencode y Gemini); la sección "Extensibilidad" ahora solo lista Codex como pendiente. Se amplió `template/AGENTS.md` (sección *Portabilidad del harness*, espejo de bootstrap) y `template/README.md` ("Herramientas soportadas hoy: opencode y Gemini") con el mismo contenido. Se re-sincronizó `template/.claude/skills/register-harness/SKILL.md` con la fuente (verificado idéntico por diff). No se creó un `.gemini/` real en este repo (los reflejos se generan al provisionar un proyecto destino, igual que se hizo con opencode).
- **Siguiente paso:** Provisionar un `.gemini/` real en un proyecto destino para validar de punta a punta la fase de provisión de Gemini (análoga a la validación ya hecha con opencode). Completar T-010 con el soporte de Codex. Sigue pendiente T-012 (re-provisionar `proyecto_prueba` en opencode).
- **Referencias:** `.claude/skills/register-harness/SKILL.md`, `template/AGENTS.md`, `template/README.md`, `template/.claude/skills/register-harness/SKILL.md`, `CLAUDE.md`, [[tasks]] T-010, T-011, [[decisions]] D-006, [[constrains]] C-002

### [2026-07-17] — Provisión opencode (T-008), bootstrap-paradoja (T-009) y README de portabilidad
- **Estado:** completado
- **Resumen:** Sesión de construcción sobre `register-harness`. **(1) T-008 — fase de PROVISIÓN para opencode:** se extendió el skill de "solo auditar" a "auditar + provisionar" con puerta de confirmación (Paso 3): skills por copia directa, agentes por traducción de frontmatter Claude→opencode (tablas concretas de campos, tools y modelos; `mode: subagent`, se eliminan `name`/`color`, `tools` como mapa booleano con `write:false`/`edit:false` en el de solo lectura). Formato opencode verificado con doc oficial vía ctx7. **(2) Dos ajustes pedidos por el usuario tras prueba real:** (a) el agente `sesion-starter` no se activaba porque su `description` no contenía frases naturales ("iniciemos la sesión") y el agente principal elegía el skill; se enriquecieron las `description` de ambos agentes con frases naturales y se marcaron como punto de entrada preferido (además, en opencode se puede forzar con `@nombre`); (b) el usuario trabaja en opencode por **suscripción** (modelos OpenAI, no API Anthropic), así que los modelos destino pasaron a `sesion-starter → openai/gpt-5.6-luna` y `sesion-closer → openai/gpt-5.6-terra`. **(3) T-009 — bootstrap-paradoja resuelta para opencode** en dos frentes: (b) se añadió a `AGENTS.md` la sección *Portabilidad del harness* (espejo resumido del procedimiento, ya que todas las herramientas leen `AGENTS.md`) que apunta al detalle canónico en `.claude/skills/register-harness/SKILL.md`; (a) la provisión ahora **copia el propio `register-harness`** a `.opencode/skills/` → opencode queda autosuficiente para re-sincronizarse solo. Se documentó la **regla de oro** (editar la fuente `.claude/`, re-sync; nunca editar el reflejo `.opencode/`) y el modo **re-sync** (sobrescribe). **(4)** Se creó `template/README.md`: guía práctica de las dos modalidades (auditar / provisionar + sub-modo re-sync), el flujo para ajustar un agente/skill desde cualquier herramienta, y cuándo cambiar el procedimiento de portabilidad (tocando skill + espejo de `AGENTS.md`). Todo lo tocado en `.claude/` se sincronizó al molde `template/.claude/` (verificado idéntico).
- **Siguiente paso:** Re-provisionar `proyecto_prueba` (re-sync) para recoger los cambios (modelos, descripciones, autosuficiencia). Luego atacar T-010/T-011 (Codex y Gemini): verificar sus mecanismos con doc oficial antes de implementar su mapa de auditoría/provisión.
- **Referencias:** `.claude/skills/register-harness/SKILL.md`, `.claude/agents/`, `template/AGENTS.md`, `template/README.md`, [[tasks]] T-008, T-009, T-012, [[decisions]] D-004, D-005, [[lessons]] L-002

### [2026-07-17] — Validación real de la auditoría `register-harness` en `proyecto_prueba`
- **Estado:** completado
- **Resumen:** No se escribió código nuevo en el harness principal; la sesión fue de validación y planificación. Se validó con éxito el skill `register-harness` (modo auditoría) en una prueba real: el usuario creó una carpeta `proyecto_prueba`, copió el contenido de `template/` (incluyendo `register-harness`) a la raíz, y ejecutó la auditoría desde Claude Code. Resultado correcto: fuente de verdad completa; `AGENTS.md` en la raíz cubierto (baseline); faltan los 4 elementos nativos de opencode (`.opencode/skills/startup-protocol/SKILL.md`, `.opencode/skills/closing-protocol/SKILL.md`, `.opencode/agents/sesion-starter.md`, `.opencode/agents/sesion-closer.md`). El patrón de auditoría queda validado de punta a punta. Se clarificó también el flujo objetivo de provisión: estando en Claude Code se ejecuta `register-harness`, que (una vez construida la fase de provisión) creará la carpeta `.opencode/` con los skills (copia directa) y los agentes (traducidos a formato opencode), y luego el usuario abre opencode y trabaja con todo nativo. Se confirmó que el nombre correcto de la carpeta es `.opencode` (no "open_code") y que hoy `register-harness` SOLO audita, no provisiona.
- **Siguiente paso:** La próxima sesión será construir la fase de PROVISIÓN de `register-harness` para opencode (T-008): crear `.opencode/skills/` copiando los `SKILL.md` y `.opencode/agents/` traduciendo el frontmatter Claude→opencode, con confirmación del usuario antes de escribir.
- **Referencias:** `.claude/skills/register-harness/SKILL.md`, `template/`, [[tasks]] T-008, [[decisions]] D-004

### [2026-07-17] — Primer versionado en git y skill `register-harness` para portabilidad multi-agente
- **Estado:** completado
- **Resumen:** Se completó el primer versionado en git: se creó un `.gitignore` agnóstico (SO, editores, secretos, logs, deps) y se hizo el primer `git push` a `https://github.com/jdrodriguez1000/Base_Harness.git` (rama `main`), commits `0a6809e` y `0f0778c`. El usuario cambió `auto_push` de `false` a `true` en `905_context/project.yaml`. Se analizó la portabilidad multi-agente del harness verificando con documentación oficial (vía ctx7) cómo funcionan Codex (skills en `.agents/skills/<n>/SKILL.md`; agentes en `~/.codex/agents/*.toml`, TOML nivel usuario; lee `AGENTS.md`) y opencode (skills en `.opencode/skills/<n>/SKILL.md`; agentes en `.opencode/agents/*.md` con frontmatter propio mode/model/prompt/tools; lee `AGENTS.md`). Se creó el skill `register-harness` (`.claude/skills/register-harness/SKILL.md`) que audita la portabilidad del harness a otra herramienta, comparando la fuente de verdad (`.claude/`) contra lo que la herramienta destino necesita, e informa qué falta (modo actual: solo auditar, no escribir; herramienta soportada por ahora: opencode). Se probó la auditoría: `.claude/` está completa; para opencode faltan 5 elementos (`AGENTS.md` en raíz, 2 skills en `.opencode/skills/`, 2 agentes en `.opencode/agents/`). Hallazgo: este proyecto no tiene `AGENTS.md` en la raíz (solo `CLAUDE.md`); el `AGENTS.md` real vive en `template/`. Se re-sincronizó `template/.claude/` con la fuente, incluyendo el nuevo `register-harness` en el molde. Se registró `D-004` (register-harness como mecanismo oficial de portabilidad/sincronización, arquitectura de dos niveles), que absorbe la antigua `T-002`.
- **Siguiente paso:** El usuario hará una prueba en la próxima sesión, en una carpeta y terminal diferentes: clonar/copiar el harness desde GitHub y probarlo. Por eso todo debe quedar commiteado y empujado antes de cerrar esta sesión. Después: implementar la fase de PROVISIÓN de `register-harness` para opencode (crear `.opencode/skills` y `.opencode/agents`, asegurar `AGENTS.md` en raíz), espejar el procedimiento de `register-harness` dentro de `AGENTS.md` para resolver la bootstrap-paradoja, y extender el soporte a Codex y Gemini.
- **Referencias:** `.gitignore`, `.claude/skills/register-harness/SKILL.md`, `template/.claude/`, `905_context/project.yaml`, [[tasks]] T-002, T-007..T-011, [[decisions]] D-004

### [2026-07-17] — Protocolos de inicio/cierre, entregable `template/` autocontenido y contexto declarativo
- **Estado:** completado
- **Resumen:** Se creó el skill `closing-protocol` y el agente `sesion-closer` (green, sonnet) que lo invoca; y el skill `startup-protocol` (solo lectura) con el agente `sesion-starter` (blue, haiku). Se actualizó el `CLAUDE.md` de la raíz para hacer obligatorios ambos protocolos. Se creó `template/` como entregable autocontenido: `AGENTS.md` (fuente única de verdad, agnóstica) con punteros `CLAUDE.md`/`GEMINI.md`, protocolos en línea, `template/_persistence/` (molde movido) y `template/.claude/` (copia de `agents` y `skills` de la raíz). Se auditó la agnosticidad de skills/agentes/template, eliminando referencias hardcodeadas a `900_persistence` (el Paso 0 ahora usa convención de nombre y luego `project.yaml`). Se creó la carpeta de contexto declarativo: `template/_context/` (molde) y `905_context/` (real, con `project.yaml` — repo, rama, `auto_push:false`, `persistence.dir` — y `business.md`). Se cableó el Paso 6 (commit & push) del `closing-protocol` para leer `project.yaml`, y el Paso 0 de ambos protocolos para leer `persistence.dir`. Se hizo `git init`, rama `main`, y se añadió `origin` apuntando a `https://github.com/jdrodriguez1000/Base_Harness.git`.
- **Siguiente paso:** Rellenar `905_context/business.md` con datos reales; mantener sincronizadas las dos copias de `.claude/` (raíz vs `template/.claude/`); confirmar con el usuario el primer `git push`.
- **Referencias:** `.claude/skills/closing-protocol/`, `.claude/skills/startup-protocol/`, `.claude/agents/`, `template/`, `905_context/project.yaml`, [[tasks]] T-002..T-006, [[decisions]] D-003, [[lessons]] L-001

### [2026-07-17] — CLAUDE.md de este proyecto
- **Estado:** completado
- **Resumen:** Se crea `CLAUDE.md` en la raíz, específico de la construcción del harness: describe el proyecto, la distinción molde/instancia (`_persistence/` vs `900_persistence/`), los 6 archivos de memoria, y 6 reglas de trabajo para el agente.
- **Siguiente paso:** Definir el archivo de instrucciones **genérico** para proyectos futuros y su estrategia de sincronización multi-agente (Claude/Codex/opencode/Gemini).
- **Referencias:** `CLAUDE.md`, [[decisions]] D-002

### [2026-07-17] — Capa de persistencia con índices y separación molde/instancia
- **Estado:** completado
- **Resumen:** Se añadió a cada archivo de persistencia una sección `## Índice` para búsqueda sin lectura completa. Se separó el molde (`_persistence/`, limpio) de la memoria real de este proyecto (`900_persistence/`). Registrado en `D-002`.
- **Siguiente paso:** Crear el `CLAUDE.md` de este proyecto.
- **Referencias:** `_persistence/`, `900_persistence/`, [[decisions]] D-002

### [2026-07-17] — Inicialización del harness base
- **Estado:** completado
- **Resumen:** Se crea la capa de persistencia con los seis archivos base (`progress`, `tasks`, `lessons`, `decisions`, `assumptions`, `constrains`).
- **Siguiente paso:** Añadir índices de búsqueda a cada archivo.
- **Referencias:** `_persistence/`
